// Route guard — placeholder.
// Redirects unauthenticated users to Login; enforces role-aware routing
// (client-side UX only — server-side RBAC is authoritative, see
// docs/System_Architecture.md Section 6).

export {};
