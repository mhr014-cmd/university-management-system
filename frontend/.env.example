# Frontend environment configuration — placeholder values only.
# Copy to .env and fill in real values. Never commit the real .env file.

# Versioned business API (added starting Milestone 1+)
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Unversioned infrastructure endpoints (currently only /health)
VITE_API_ROOT_URL=http://localhost:8000
