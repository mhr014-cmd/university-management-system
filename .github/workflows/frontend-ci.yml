# Frontend CI — placeholder workflow.
# Intended to run lint + build + tests for frontend/ on push/PR.
# To be implemented during Milestone 11 (see docs/Implementation_Roadmap.md).

name: Frontend CI

on:
  push:
    paths:
      - "frontend/**"
  pull_request:
    paths:
      - "frontend/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      # - name: Set up Node
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: "20"
      # - name: Install dependencies
      #   run: npm install --prefix frontend
      # - name: Lint
      #   run: npm run lint --prefix frontend
      # - name: Build
      #   run: npm run build --prefix frontend
      - name: Placeholder
        run: echo "Frontend CI not yet implemented — see docs/Implementation_Roadmap.md Milestone 11."
