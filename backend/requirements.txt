# Backend dependencies. Fixed technology choices per
# docs/System_Architecture.md Section 12. Pinned to major.minor floors for
# a Python 3.12 target; exact patch versions resolved by pip at install time.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg2-binary>=2.9,<3.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-jose[cryptography]>=3.3,<4.0   # JWT handling (Milestone 2)
passlib[bcrypt]>=1.7,<2.0             # password hashing (Milestone 2)
python-multipart>=0.0.9               # form/file upload support (profile photo, etc.)
python-dotenv>=1.0,<2.0

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
httpx>=0.27,<1.0

# PDF generation (library choice not fixed by proposal — see
# docs/System_Architecture.md Section 12, to be finalized in Milestone 7/8)
