# University Management System — Project Progress

Single source of truth for milestone-level progress. Milestone numbering, names, and order are copied verbatim from `docs/Implementation_Roadmap.md` (the approved, frozen build order) — this file tracks *status against* that plan, it does not redefine it. For each milestone's Goal, Files, APIs, DB tables, Frontend pages, and Dependencies, see the roadmap directly. For per-requirement status (Testing/Implementation/Verification), see `docs/Requirement_Traceability_Matrix.md`.

**Last updated:** 2026-07-23 (Milestone 11 — Hardening, Testing & Deployment — completed; project's final milestone)

---

## Summary

| Field | Value |
|---|---|
| **Overall Progress** | 100% (12 of 12 milestones completed) |
| **Current Milestone** | M11 — Hardening, Testing & Deployment *(complete, awaiting explicit approval — see Review Status below)* |
| **Last Completed Milestone** | M11 — Hardening, Testing & Deployment |
| **Next Milestone** | None — M11 is the final milestone in `Implementation_Roadmap.md`. |
| **Current Git Commit** | *(pending — see Milestone 11 entry's Commits line)* |

**Schedule risk (from `Implementation_Roadmap.md`):** full 12-milestone scope is ~20 working days solo against the July 13, 2026 deadline (10-day runway from project start). Planned Dates below are computed from the roadmap's own cumulative day estimates and show M6 onward landing **after** July 13 — this is the same risk the roadmap already flags, not a new finding. Committed core is M0–M7 + M11; M8 (Fees) and parts of M10 (advanced reporting) are the first items to cut if the timeline slips further.

---

## Milestone Tracker

| Milestone | Status | Planned Date | Completed Date | Git Commit Hash | Review Status | Notes |
|---|---|---|---|---|---|---|
| **M0** — Project Scaffolding & Environment Setup | Completed | 2026-07-03 | 2026-07-03 | `8cb72c1` | **Approved** | Backend app factory, DB/Alembic wiring, `/health`, frontend shell all verified working (see Milestone Detail Log). Dependency-pin defect found and fixed post-hoc (`fdaaf59` → `889465e`). Full 9-document self-review run (`5fe42ca`, `cfef1e8`) — found and fixed two gaps. Reproducibility verified from a genuinely fresh `git clone`. `HTTP_422_UNPROCESSABLE_ENTITY` deprecation warning fixed (`8cb72c1`). **Approved by user 2026-07-04.** |
| **M1** — Core Reference Data Model | Completed | 2026-07-04 | 2026-07-04 | `37f3ce4` | **Approved** | Department/Course/Room/Semester models, schemas, repository, service, router (12 endpoints, list/create/get-by-id per entity) implemented exactly per `Database_Design.md` §6.7/6.8/6.11/6.20 and the new `API_Contract.md` §10. Alembic revision `0002_core_reference_data`. **Post-completion fix:** user-reported real-machine failure — `cd backend && alembic upgrade head` raised `ModuleNotFoundError: No module named 'app'` because verification had only ever used `python -m alembic`, not the bare console script. Fixed via `prepend_sys_path` + a `__file__`-based fallback in `env.py`; reproduced-then-fixed-then-reverified from a fresh clone. `MILESTONE_VERIFICATION_CHECKLIST.md` updated so this can't recur. Known issues from M1 (endpoints unauthenticated; migration not autogenerate-diff-checked) both addressed/closed in M2 — see M2 row and Milestone Detail Log. **Approved by user 2026-07-04.** |
| **M2** — Authentication & Authorization | Completed | 2026-07-05 | 2026-07-04 | `b26024f` | **Approved** | JWT access/refresh auth (login/refresh/logout/change-password), bcrypt password hashing, `require_roles` RBAC dependency retrofitted onto all 12 M1 reference-data endpoints, and the full frontend auth module (token storage, interceptors, AuthContext, RouteGuard, Login page). Alembic revision `0003_user`. 41 backend tests added (previously zero existed project-wide) covering BR-006, VR-002, refresh-token rotation/reuse, and RBAC negative cases. Independent user review found and requested four fixes (ORM/migration metadata parity, doc gaps) — all resolved in `b26024f`; a separate `.env`-drift audit (unrelated to any code change made by this project's automation) was also conducted and explained. **Approved by user, git tag `v0.3-milestone2`.** |
| **M3** — User Management & Profiles (Student, Teacher, Parent, Admin) | Completed | 2026-07-07 | 2026-07-05 | `5fe89cb` | **Approved** | Role-specific profile tables (Student/Teacher/Parent/Admin/ParentStudentLink), self-service `GET`/`PUT /users/me` (VR-009 enforced structurally), Admin-driven Student/Teacher account lifecycle (FR-009–FR-016, BR-006 soft deactivation), `seed_admin.py` bootstrap script, and the Profile + Admin: User Management frontend pages. Alembic revision `0004_role_profiles` (corrected from the roadmap's stale `0003_role_profiles` reference). 49 new tests (72 total passing). See Milestone Detail Log for the two scope decisions confirmed with the user before implementation (Parent account creation/linking deferred; no invite-based provisioning) and the real `pyproject.toml`/`requirements.txt` drift found and fixed. **Approved by user, git tag `v0.4-milestone3`.** |
| **M4** — Scheduling & Timetable | Completed | 2026-07-09 | 2026-07-06 | `1ddaaf5` | **Approved** | `ClassSession`/`Enrollment`/`ScheduleEntry`/`ScheduleChangeRequest` (FR-045–FR-051, BR-004, BR-005, VR-007), real interval-overlap conflict detection (not just the DB-level exact-duplicate constraint), the Teacher-request/Admin-resolve change workflow, and the two Derived `POST /schedule/class-sessions`/`POST /schedule/enrollments` endpoints confirmed with the user during pre-implementation review. Alembic revision `0005_scheduling` (corrected from the roadmap's stale `0004_scheduling` reference — part of a roadmap-wide migration-numbering fix, see Milestone Detail Log). 27 new tests (99 total passing). Timetable frontend page (role-branched: Student/Teacher weekly grid, Admin schedule-management panel). **Approved by user, git tag `v0.5-milestone4`.** |
| **M5** — Attendance | Completed | 2026-07-11 | 2026-07-07 | `611281c` | **Approved** | `AttendanceRecord` (FR-026–FR-032, BR-007, BR-008, VR-005) with all 10 mandatory Attendance Domain Rules enforced explicitly at the service layer before any write, plus the Derived `GET /schedule/class-sessions/{id}/roster` endpoint confirmed with the user during pre-implementation review. Alembic revision `0006_attendance`. Low-attendance warning threshold (80%) and percentage formula both resolved as documented assumptions (see Milestone Detail Log). 35 new tests (134 total passing). Attendance (Student) and Teacher: Attendance Marker frontend pages. **Approved by user, git tag `v0.6-milestone5`.** |
| **M6** — Exams & Grading | Completed | 2026-07-15 | 2026-07-15 | `0c3fe44` | **Approved** | `Exam`/`Question`/`QuestionOption`/`ExamSubmission`/`Answer`/`QuestionGrade` (FR-017–FR-025, BR-001, BR-003, VR-003, VR-004, VR-006) with all 12 mandatory Examination Domain Rules enforced explicitly at the service layer, plus two Derived endpoints confirmed with the user: `POST /exams/{id}/start` (server-clock-only exam timing, found during pre-implementation review) and `GET /exams/{id}/submissions/{submission_id}` (per-answer grading detail, found during frontend implementation). Alembic revision `0007_exams`. 77 new tests (211 total passing). Exam List, Exam Room, Teacher: Exam Builder, Teacher: Grading Interface frontend pages. **Approved by user, git tag `v0.7-milestone6`.** |
| **M7** — Results & Transcripts | Completed | 2026-07-17 | 2026-07-17 | `2af8744` | **Approved** | `Result` (FR-033–FR-037, BR-002) with all 15 mandatory Results & Academic Records Domain Rules enforced explicitly at the service layer, plus two Derived additions confirmed with the user: `result.exam_id` (schema, found pre-implementation) and `GET /results/pending` (endpoint, found during frontend implementation). Alembic revision `0008_results`. Credit-hour-weighted GPA (resolves `Requirement_Analysis.md` §14 item 6). PDF transcript generation via `reportlab`. 42 new tests (253 total passing). Results View and Admin: Result Approval frontend pages (Student-facing only — see Milestone Detail Log for the Parent-UI known gap). **Approved by user, git tag `v0.8-milestone7`.** |
| **M8** — Fees (Optional) | Completed | 2026-07-19 | 2026-07-19 | `8258ea7` | **Approved** | `FeeStructure`/`Invoice`/`Payment` (FR-038–FR-043, VR-008) with all 17 mandatory Fees Domain Rules enforced explicitly at the service layer. `POST /fees` auto-generates one invoice per eligible enrolled student (resolved during pre-implementation review, confirmed with the user — no separate invoice-creation endpoint exists). Overpayment strictly disallowed, resolving VR-008's previously unresolved policy. Alembic revision `0009_fees`. PDF invoice generation via `reportlab`. 40 new tests (293 total passing). Fee Centre and Admin: Fee Dashboard frontend pages (Student-facing only; Notify/Bulk-notify actions deferred to M9 — see Milestone Detail Log). **Approved by user, git tag `v0.9-milestone8`.** |
| **M9** — Notifications | Completed | 2026-07-21 | 2026-07-21 | `04eef50` | **Approved** | `Notification` (FR-031, FR-044, FR-051–FR-053) with all 20 mandatory Notification Domain Rules enforced explicitly. Four automatic dispatch triggers (`result_published`, `schedule_change`, `attendance_warning`, `fee_due`) wired into the four originating services' commit paths, confirmed with the user on two ambiguities: attendance-warning fires only on a genuine `>=80%`-to-`<80%` threshold crossing, and no new notification type was added for exam-domain events (`result_published` covers it). One documentation fix: schedule-change recipients corrected to include the assigned Teacher, per `UI_Wireframes.md`. Alembic revision `0010_notifications`. 25 new tests (318 total passing). Notifications feed page and a persistent bell icon with an unread-count badge. **Approved by user, git tag `v1.0-milestone9`.** |
| **M10** — Dashboards & Reporting | Completed | 2026-07-22 | 2026-07-22 | `620c67f` | **Approved** | `GET /results/reports`, `GET /fees/reports` (new `report_service.py`/`routers/reports.py`), `POST /fees/overdue/notify` (reuses the existing Notification Dispatcher — no second notification system), and role-specific Dashboard widgets for all four roles plus the Admin: Reports page (Attendance/Results/Fees tabs), resolving five pre-implementation findings (A–E) with the user before coding: (A) additive `average_gpa` on `GET /results/reports`, reusing the existing GPA formula; (B) Teacher Pending Grading computed client-side, no new endpoint; (C) Teacher Schedule-change-request widget omitted (no endpoint exists), documented as a known limitation; (D) additive `created_at` on `GET /users/students`/`GET /users/teachers`; (E) Parent Dashboard limited to Fee Status/Recent Results, honest "Not available" states elsewhere. The temporary Dashboard health-check widget (flagged in the M0 traceability review) was removed now that real role-specific widgets exist. No new database tables/migrations. 23 new tests (341 total passing). **Approved by user, git tag `v1.1-milestone10`.** |
| **M11** — Hardening, Testing & Deployment | Completed | 2026-07-23 | 2026-07-23 | *(pending)* | Pending | Final milestone: rate limiting on `POST /auth/login` (5/60s per IP, in-memory), `/docs`/`/redoc`/`/openapi.json` disabled when `ENVIRONMENT=production`, root frontend error boundary, minimal ESLint flat config, 7 new frontend component tests (exam timer, grading form, approval workflow — CLAUDE.md §10's own named examples), `backend/scripts/seed_demo_data.py` fully implemented per `Database_Design.md` §11, both GitHub Actions CI workflows wired to real pipelines (replacing Milestone 0 placeholders), and the orphaned `Parent: Child View` placeholder removed as dead code (its underlying requirement remains tracked as a permanent, accepted limitation, not silently dropped). No new database tables/migrations/business endpoints. 8 new backend tests (349 total passing) + 7 new frontend tests. |

*Planned Dates are derived from `Implementation_Roadmap.md`'s cumulative day estimates (Summary Table), added sequentially to the project start date (2026-07-03) — they are a scheduling projection, not a commitment, and will drift as actual completion dates land. Update this table's Planned Date column if the roadmap's estimates are revised.*

---

## Milestone Detail Log

*(One entry per completed milestone — brief summary of what actually shipped, plus commit references. Add a new entry each time a milestone closes; do not delete prior entries.)*

### M0 — Project Scaffolding & Environment Setup (Completed — 2026-07-03)

FastAPI app factory (settings-driven config with fail-fast validation, structured JSON logging, CORS, global exception handlers producing the standard error envelope, request logging middleware, `/health` with DB connectivity check), SQLAlchemy engine/session wiring, Alembic configured against `Base.metadata` with an empty baseline revision. React 18 + TypeScript + Vite + TailwindCSS frontend with React Router, React Query, a split Axios client (versioned + root), a light/dark theme provider, a shared app layout, and a live backend-connectivity widget on the Dashboard placeholder. Docker Compose local dev stack (Postgres + backend reload + Vite dev server). No business modules, no authentication — strictly foundation, per the milestone's own constraints.

Verified (not just claimed): clean `pip install`/`npm install`, app imports and boots, `uvicorn` serves real HTTP, `GET /health` and `GET /openapi.json` both return correctly, `npx tsc --noEmit` and `npm run build` both succeed, and a real defect (404s bypassing the custom exception handler due to a `fastapi.HTTPException` vs `starlette.exceptions.HTTPException` registration mismatch) was found and fixed during verification.

Two follow-up fix passes after initial completion:
- Dependency pins tightened from floating ranges to exact versions (`fdaaf59`), then re-verified against two independent clean dependency resolutions after a reviewer flagged `starlette==1.3.1` as suspicious-looking — confirmed legitimate via PyPI's JSON API and FastAPI's own GitHub release notes, not a bad resolution (`889465e`).
- API display metadata (OpenAPI title/description, Swagger, startup log banner) renamed from "ICT Education API" to "University Management System API" (`9e9680d`).

**Commits:** `71019ef` (backend foundation), `8c9e25f` (frontend foundation), `7eff95d` (infra), `fdaaf59` (dependency pins v1), `889465e` (dependency pins v2 — verified fix), `9e9680d` (metadata rename).

#### Full documentation self-review (2026-07-03, per `CLAUDE.md` §14 item 12)

Ran `docs/MILESTONE_VERIFICATION_CHECKLIST.md` Section 13 against all nine governing documents. Two real gaps found and fixed (documentation-only, no implementation changed):
- **Undocumented frontend additions** — the theme toggle and Dashboard health widget had no proposal linkage and no `Proposal_vs_Engineering_Additions.md` entry. Fixed in `07695ab`: both logged as Design Enhancements, theme toggle kept permanently, health widget scheduled for removal at M10.
- **Undocumented backend middleware/utilities** — exception handlers, request logging middleware, structured logging config, settings validation, DB session/engine, CORS, and the Alembic baseline had no entry either, despite the same-commit policy (added the commit before this review) explicitly naming "middleware, utility." Fixed in `5fe42ca`: all seven logged as Derived (unavoidable prerequisites for Required features, each implementing a mechanism `System_Architecture.md` already mandates).

Also performed **live verification beyond the original completion pass** (which had used `TestClient`, not a real browser): started the actual backend (`uvicorn`, port 8010) and frontend (`vite`, port 5173) via `.claude/launch.json`, in a real browser —
- Zero console errors; only Vite HMR debug logs and a harmless React Router v6 future-flag notice
- `GET http://localhost:8010/health` succeeded cross-origin from the frontend (confirms CORS actually works end-to-end, not just in unit tests) — real network tab evidence, response `{"status":"degraded","database":"unreachable"}` (correct — no live Postgres in this environment)
- Login (`/`) and Dashboard (`/dashboard`) pages render correctly; theme toggle clicked and confirmed switching light/dark and updating its own label
- No untracked/stray files left behind — temporary `.env` files used for this test were removed afterward; `.claude/launch.json` added as a reusable (gitignored) tool for future milestones' Section 8/13 checks

Both `PROJECT_PROGRESS.md`'s Summary and this milestone's row were stale before this review (`Current Git Commit` pointed at `d9ff4d2`, several commits behind actual HEAD) — corrected as part of this same pass.

#### Clean-clone reproducibility verification (2026-07-03)

Everything above had been verified from the working tree, which can accumulate local state (stale `.venv`, cached wheels, manually-edited files) that masks a real reproducibility problem. Re-verified from a genuinely fresh `git clone` (not a copy) into an isolated temp directory, at commit `f4044d5`:

| Step | Result |
|---|---|
| `git clone` (local) | Only tracked files present — confirmed no `.venv`, `.env`, `node_modules`, `.claude/`, or build artifacts came along |
| `python -m venv .venv` + `pip install --no-cache-dir -r requirements.txt` | Clean install, exit 0, **no pip cache used at all** |
| `pip check` | "No broken requirements found" |
| Resolved package set vs. `requirements.txt` pins | Exact match, all 46 packages (verified programmatically with PEP 503 name normalization, not eyeballed) |
| `cp .env.example .env` (no manual edits) | — |
| `alembic upgrade head` | **Alembic mechanism confirmed working** (loads `env.py`, resolves settings, attempts connection) — **connection itself failed**: the documented placeholder `DATABASE_URL` (`user`/`password`) doesn't match this machine's real local PostgreSQL 18 instance, whose actual credentials are unknown to me. Deliberately did not brute-force guess further after two reasonable attempts (`postgres`/`postgres`, `postgres`/`password`) both failed — that would be inappropriate against a real local database. **Known limitation, not a defect**: M0's only migration is an empty baseline (no schema to verify), so the meaningful check — does the Alembic wiring itself work — passed; a genuine `upgrade head` success run still needs to happen once, with real credentials, before M1 adds actual schema. |
| `uvicorn app.main:app` (real process, real port, real HTTP via `curl`) | `GET /health` → `200 {"status":"degraded","database":"unreachable"}` (correct graceful degradation, matching working-tree behavior exactly); `GET /openapi.json` → 200, title correct |
| `npm ci` (lockfile-exact install, not `npm install`) | Clean, same known esbuild/vite dev-server advisory as previously logged (Known Issue, unchanged) |
| `npx tsc --noEmit` | Zero errors |
| `npm run build` | Succeeds, same output shape as working-tree build |
| `npm run dev` (real Vite process, real port) | Boots in 263ms; `curl` confirms `index.html` and `/src/main.tsx` both serve correctly, title tag correct |
| Backend + frontend paired live (different clean-clone ports) | `GET /health` reachable from the frontend's configured API base — full stack wired correctly end-to-end |
| Cleanup | All test processes stopped by exact PID (command-line matched, not by name — avoided touching unrelated `python`/`node` processes on the machine); temp clone directory removed entirely; main working tree confirmed untouched (`git status` clean) throughout |

**Conclusion: Milestone 0 is reproducible from a clean clone**, with one explicitly-scoped exception: full `alembic upgrade head` execution success against a live database has not been demonstrated in this sandbox (credentials for the one reachable local Postgres instance are unknown, and no Docker is available here to spin up a throwaway one). Recommended next step: run `docker compose up` (per `README.md`) or supply real `DATABASE_URL` credentials once, specifically to confirm migration execution.

**Milestone 0 approved by user, 2026-07-04.**

### M1 — Core Reference Data Model (Completed — 2026-07-04)

Implemented exactly the scope `Implementation_Roadmap.md` defines for Milestone 1 — Department, Course, Room, Semester — and nothing from Milestone 2 or later.

**Database:** SQLAlchemy models for all four tables, matching `Database_Design.md` §6.7/6.8/6.11/6.20 column-for-column (including that `room` and `semester` deliberately have no `created_at`/`updated_at`, per the spec — not an oversight). Alembic revision `0002_core_reference_data` (down_revision `0001`): creates all four tables with the documented unique constraints (`department.name`, `department.code`, `course.code`, `room.name`, `semester.name`), the `course.department_id` FK with `ON DELETE RESTRICT`, the `course.department_id` index (per `Database_Design.md` §9), and the `semester.start_date < end_date` check constraint (§10).

**API:** 12 endpoints — list/create/get-by-id for each of Department, Course, Room, Semester — newly documented in `API_Contract.md` §10 *before* implementation (per the same-commit engineering-additions policy), classified **Derived** in `Proposal_vs_Engineering_Additions.md` (unavoidable plumbing for Required features, not a proposal feature itself). Update/delete deliberately not implemented — nothing yet needs to edit or remove reference data; scope stays minimal rather than preemptive.

**Layering:** router → service → repository, per `CLAUDE.md` §6 — routers contain no queries, services own uniqueness/FK-existence checks and translate `IntegrityError` to 409, repositories hold all SQLAlchemy statements.

**Known issues (both explicitly tracked, not oversights):**
1. **Endpoints are unauthenticated.** Milestone 1 lands before Milestone 2 (Authentication & Authorization) per the roadmap's own dependency graph — there is no RBAC mechanism yet to apply. Documented in `API_Contract.md` §10's header note and in `Proposal_vs_Engineering_Additions.md`. RBAC is added when M2 lands; this is not a security defect in the delivered scope, since M1 was never going to have auth available.
2. **Migration hand-authored, not `--autogenerate`'d.** Same sandbox limitation as Milestone 0 (no known-good local Postgres credentials, no Docker available) — autogenerate requires a live DB connection to diff against. The migration was written to mirror the SQLAlchemy models column-for-column and reviewed carefully, but a real `alembic revision --autogenerate` diff-check against a live database (expected to show *no* changes if this migration is correct) has not been performed. Recommended before this migration is trusted in a real deployment.

**Real defect found and fixed during verification (not deferred):** Pydantic v2's error details for custom `@model_validator` failures (used by `SemesterCreate` for the `start_date < end_date` check) embed a raw, non-JSON-serializable exception instance in `error["ctx"]["error"]`. The shared `validation_exception_handler` in `app/middleware/error_handlers.py` (a Milestone 0 file) was passing this straight into `json.dumps()`, causing a `TypeError` that surfaced as an unhandled 500 instead of the documented 422. Fixed by routing `exc.errors()` through `fastapi.encoders.jsonable_encoder` before building the response. This is a Milestone-0-file fix made during Milestone 1 because M1's own documented behavior (`API_Contract.md` §10.11: "start_date >= end_date (422)") depended on it — not scope creep into Milestone 2.

#### Post-completion fix: `alembic upgrade head` failed on a real machine (2026-07-04)

User-reported, reproduced, and fixed. `cd backend && alembic upgrade head` (the bare console-script entry point — how a real developer actually runs it) failed with `ModuleNotFoundError: No module named 'app'` from `backend/alembic/env.py`. Root cause: my own verification throughout Milestone 0 and Milestone 1 only ever invoked Alembic via `python -m alembic` (which implicitly adds the current working directory to `sys.path`) or through direct Python imports (`from app.main import app`) — never the plain `alembic` command, which does **not** add cwd to `sys.path`. This is a genuine gap in the verification methodology itself, not just the code.

**Fix:** `prepend_sys_path = .` added to `alembic.ini` (the standard Alembic mechanism for exactly this problem — interpreted relative to the ini file's own location, correct regardless of invocation cwd), plus a `__file__`-based `sys.path` insertion directly in `env.py` as a self-contained fallback.

**Verified properly, not just patched:** reproduced the exact failure first (fresh `git clone`, fresh venv, bare `alembic` console script — confirmed identical error/traceback to the report), applied the fix, confirmed `alembic upgrade head` now proceeds past the import and reaches a real database connection attempt (fails only on placeholder credentials, the same pre-existing documented limitation, not a new bug), confirmed `alembic history`/`alembic current` work, and confirmed `python -m alembic` still works with no regression.

**Process fix, not just a code fix:** `docs/MILESTONE_VERIFICATION_CHECKLIST.md` §4 updated to require testing the bare console-script form specifically — testing only `python -m alembic` is no longer sufficient evidence for future milestones.

**No application architecture changed** — fix is scoped entirely to `alembic.ini` and `alembic/env.py` import/path resolution.

**Verified:** fresh `pip install`, all new modules syntax-checked, app imports and boots, all 12 endpoints present in `/openapi.json` under `/api/v1`, field-validation (422) and semester date-order validation (422, post-fix) both produce the standard error envelope, duplicate-name/code paths correctly designed to return 409 (exercised at the service-logic level; full round-trip against a live DB blocked by the same credentials limitation as the migration), graceful 500 (not a crash) when the database is unreachable, and all Milestone 0 behaviors (`/health`, 404 shape, app title) remain unchanged.

**Outstanding before M2 can start:** explicit approval of M1. **Approved by user 2026-07-04.**

### M2 — Authentication & Authorization (Completed — 2026-07-04)

Implemented exactly the scope `Implementation_Roadmap.md` defines for Milestone 2: JWT-based login/refresh/logout/change-password, RBAC middleware, and its retrofit onto Milestone 1's endpoints. Nothing from Milestone 3 (account creation, user profiles) was implemented — `UserRepository` deliberately has no `create()` method.

**Design gap resolved first (docs-only, `414bc30`):** `Database_Design.md`'s `user` table had no mechanism to support refresh-token rotation/revocation. Presented the tradeoff to the user via `AskUserQuestion` (a dedicated session table vs. two columns on `user`); user chose the two-column approach. `current_refresh_token_jti`/`refresh_token_expires_at` added to `Database_Design.md` §6.1 with an explicit design note: this is a single-active-session-per-user model — logging in on a second device invalidates the first session's refresh token. `API_Contract.md` updated to reference the resolved design.

**Database:** `user` table (`id`, `email` unique, `password_hash`, `role` enum, `is_active`, the two refresh-tracking columns, `created_at`/`updated_at`) matching `Database_Design.md` §6.1 exactly. Alembic revision `0003_user` (down_revision `0002`).

**Backend:** `app/core/security.py` (bcrypt hashing, JWT encode/decode), `app/repositories/user_repository.py`, `app/services/auth_service.py` (BR-006 deactivation check on both login and refresh; refresh-token rotation with jti-mismatch reuse detection; VR-002 change-password check), `app/middleware/auth.py` (`get_current_user`, re-checks `is_active` on every request per `CLAUDE.md` §12) and `app/middleware/rbac.py` (`require_roles`), `app/routers/auth.py` (4 endpoints under `/api/v1/auth`). RBAC retrofitted onto all 12 M1 reference-data endpoints (GET: any authenticated role; POST: Admin), closing M1's tracked known issue.

**Frontend:** `frontend/src/auth/` (tokenStorage, AuthContext, RouteGuard), Axios request/response interceptors in `lib/apiClient.ts` (Bearer attachment, silent refresh-and-retry on 401), a real Login page, logout action + user email display in `AppLayout`. localStorage chosen for token persistence over httpOnly cookies — documented tradeoff (the API returns tokens in the JSON body, not `Set-Cookie`; an httpOnly-cookie approach would need a backend-for-frontend layer this project doesn't have).

**Real defects found and fixed during verification (not deferred):**
1. `passlib==1.7.4` (unmaintained, final release) crashes against `bcrypt>=4.1` — probes a removed `bcrypt.__about__.__version__` attribute. Replaced with direct `bcrypt.hashpw`/`checkpw` calls (`ce6a5af`).
2. Alembic migration `0003_user`: redundant explicit `user_role.create()` in `upgrade()` conflicted with `op.create_table()`'s automatic enum creation (`DuplicateObject`); and `op.drop_table()` was confirmed to *not* drop the enum type on its own, so the explicit `.drop()` in `downgrade()` is required and was kept. Found via the first genuine `alembic upgrade`/`downgrade`/`upgrade` cycle run against a real, disposable PostgreSQL database in this project's history (`8bbc009`).
3. Frontend: the response interceptor's silent-refresh logic was hijacking `/auth/login`'s own 401 (wrong password) — no refresh token exists yet, so the doomed refresh attempt force-navigated to `/login`, wiping the login form's error state before the user ever saw it. Fixed by excluding `/auth/login`/`/auth/refresh` from the refresh-and-retry flow (`42aaac6`).
4. `backend/tests/` had zero test files project-wide (`pytest` reported "no tests ran") despite `CLAUDE.md` §10's requirement that every BR-xxx/VR-xxx rule have a test — caught during this milestone's own self-review, not by the user. Added 41 tests: unit tests for `core/security.py` and `AuthService` (repository stubbed, no DB dependency, always run), plus integration tests for the auth router and the RBAC retrofit (full request→DB→response against a disposable Postgres database, gated on `TEST_DATABASE_URL`, skipped rather than failing when unavailable) (`494b60f`).

**Verified (not just claimed):** `pytest` — 41 passed (23 unit + 18 integration) against a disposable `ict_education_m2_test` database, created and dropped solely for this run. `npx tsc --noEmit` and `npm run build` both clean. `pip check` — no broken requirements. `alembic upgrade head` / `downgrade -1` / `upgrade head` all clean via the bare `alembic` console-script entry point (the specific historical failure mode from M1) against a fresh disposable database. Full live-browser verification of the frontend auth flow (wrong-password error banner, successful login + redirect + user display, logout, direct-navigation route guarding while unauthenticated) via the preview browser tools against a real backend + real Postgres, using `.claude/launch.json`'s `env` field so no `.env` file was ever touched. All disposable test/verification databases (`ict_education_m2_verify`, `ict_education_m2_test`, `ict_education_m2_migration_check`) dropped after use; the developer's real `university_management_db` was never touched.

**Known issues (tracked, not oversights):**
1. ~~Migration `0003_user` not autogenerate-diff-checked.~~ **Resolved in the M2 review follow-up below** — `alembic revision --autogenerate` now confirmed to produce an empty migration.
2. FR-005 (redirect to role-specific dashboard) currently redirects every role to the same generic `/dashboard` — role-specific dashboard *content* is out of scope until the role-owning domains (M3+) exist. Login/redirect mechanism itself is complete and tested.
3. Single-active-session-per-user is a deliberate scope decision (see design-gap resolution above), not a defect — logging in on a second device invalidates the first device's refresh token (the first device's access token remains valid until it naturally expires, then its silent-refresh attempt will fail and redirect to `/login`).

**Commits (`37f3ce4`..`494b60f`):** `414bc30` (design gap resolution, docs-only), `1ed5a68` (User model + migration), `ce6a5af` (security utilities), `affe337` (auth schemas), `8bbc009` (migration bug fix), `99b13df` (user repository + auth service), `c600ff3` (auth + RBAC middleware), `c95c6e9` (auth router), `23f7bff` (RBAC retrofit on M1 endpoints), `42aaac6` (frontend auth module), `494b60f` (backend test suite).

#### M2 review follow-up (2026-07-05)

Before final approval, the user ran an independent verification pass (see prior conversation turn) and asked for four fixes, none of which touch business logic, auth flow, JWT handling, API endpoints, tests, or milestone scope:

1. **`user.is_active` ORM/migration default asymmetry.** The migration declared `server_default=sa.true()`; the model declared only the Python-side `default=True`, with no `server_default`. Alembic doesn't compare `server_default` by default, so this never surfaced as an autogenerate diff — but it was a real, undocumented inconsistency between the two representations of the same column. **Resolved by making the model match the migration**: added `server_default=true()` to `User.is_active` alongside the existing `default=True` (both are useful — `default` gives an ORM-constructed object a value before it's ever flushed; `server_default` is what a raw `INSERT` outside the ORM relies on). No migration change needed — the DDL was already correct; only the model was updated to represent it accurately.
2. **Index representation gap.** `alembic revision --autogenerate` logged `Detected removed index` for `ix_course_department_id` and `ix_user_role` — both exist in the database (created via explicit `op.create_index()` in the hand-written migrations) but weren't declared on the corresponding model columns via `index=True`, so the ORM metadata didn't actually represent the live schema. **Resolved by adding `index=True`** to `Course.department_id` and `User.role` (both already required by `Database_Design.md` §9, which lists both indexes — the model was simply incomplete, not the design). No migration change — the indexes already existed correctly; the migrations gained a one-line comment cross-referencing the model for future readers. Re-verified with a fresh `alembic revision --autogenerate` run: generated migration body is now empty (`pass`/`pass`), with no "detected" log lines at all. Temporary diff-check migration file deleted; disposable database dropped.
3. **Ownership-check wording ambiguity.** `Implementation_Roadmap.md`'s Milestone 2 Goal/file-list describes `rbac.py` as a "role + ownership check dependency," but M2 has no `/me`-scoped or parent-linked endpoints to check ownership against — those first appear in Milestone 3. `rbac.py` has always correctly implemented role-only checks (ownership belongs in the service layer per `CLAUDE.md` §6), but this wasn't explicitly reconciled against the roadmap's own wording anywhere. **Resolved by adding a Milestone 2 scope note directly to `Implementation_Roadmap.md`** clarifying that the "ownership-check" phrase describes the milestone's eventual purpose in the dependency chain, not something M2 itself delivers, and that ownership checks land at the service layer of whichever future milestone first introduces an ownership-scoped resource. No code changed — `rbac.py`'s existing behavior was already correct.
4. **`CHANGELOG.md` missing documentation-only M2 changes.** See `CHANGELOG.md`'s `[Unreleased]` section, "Changed (Milestone 2 — Documentation)" — now lists every `Database_Design.md`, `API_Contract.md`, `Proposal_vs_Engineering_Additions.md`, `Requirement_Traceability_Matrix.md`, and `Implementation_Roadmap.md` change made during M2, not just the code changes.

**Re-verification after these fixes:** `alembic upgrade head` clean from a fresh disposable database; `alembic revision --autogenerate` produces an empty migration (confirmed no diff, including for the two previously-unrepresented indexes); temporary diff-check migration file and disposable database both removed. No business logic, auth flow, JWT implementation, API endpoint, or test file was touched.

**Full documentation self-review run** (`CLAUDE.md` §14 item 12, `MILESTONE_VERIFICATION_CHECKLIST.md`): `Database_Design.md` and `API_Contract.md` updated in the same change as the design-gap resolution (not backfilled). `Requirement_Traceability_Matrix.md` FR-001–FR-005 and NFR-001/NFR-004 updated to Verified. `Proposal_vs_Engineering_Additions.md`'s M2 schema addition and reference-data auth-note entries confirmed present and accurate. No undocumented additions found. One real gap found and fixed by this review itself: the missing test suite (see Real Defects #4, above) — fixed in the same pass, before this milestone was marked Completed, per the checklist's own instruction not to mark Completed with a known, unfixed finding.

**Outstanding before M3 can start:** explicit approval of M2. **Approved by user, git tag `v0.3-milestone2`.**

### M3 — User Management & Profiles (Completed — 2026-07-05)

Implemented exactly the scope `Implementation_Roadmap.md` defines for Milestone 3: role-specific profile tables, self-service profile endpoints, and Admin-driven account lifecycle for Students and Teachers. Nothing from Milestone 4 (Scheduling) or later was implemented — verified by inspecting every M4+ model/schema/router/page file before starting and confirming all were still M0's empty placeholders (`"""Placeholder module — no implementation yet."""`), not real code.

**Pre-implementation review, before any code was written:** read all ten governing documents and the full existing codebase, then reported inconsistencies per the user's explicit instruction to stop before coding. Findings and resolutions:
1. `Implementation_Roadmap.md`'s M3 file list named the migration `0003_role_profiles.py` — already consumed by M2's own `0003_user.py` (the identical class of stale-reference bug fixed once before for M2's migration filename). Corrected to `0004_role_profiles.py` in the roadmap and the actual migration, in the same change.
2. The roadmap named a `schemas/user_profile.py` placeholder that Milestone 0 had actually scaffolded as `schemas/user.py` — used the real filename, corrected the roadmap reference.
3. **Two genuine, proposal-level ambiguities required a stop-and-ask before coding** (both resolved via `AskUserQuestion`, user selected the recommended option in each case):
   - **Parent account creation & `parent_student_link` population:** no endpoint anywhere in `API_Contract.md` or the roadmap's M3 API list creates a Parent account or links one to a Student — `Requirement_Analysis.md` §14 item 8 already flags this as an unresolved proposal gap. **Decision:** the `parent`/`parent_student_link` tables are created and migration-verified in M3 (required by `Database_Design.md` and later milestones' `BR-007`/`NFR-003` needs) but ship with zero REST endpoints and no seed data in this milestone — seeding is `seed_demo_data.py`'s job (a later milestone per the roadmap, not M3's `seed_admin.py`). Documented in `Implementation_Roadmap.md`'s new Milestone 3 scope note and in `app/models/parent.py`'s docstring.
   - **Invite-based account provisioning:** `API_Contract.md` allows omitting the password "if invite-based provisioning is used," but no invite/email-dispatch mechanism exists anywhere (that would be Milestone 9, Notifications, scope). **Decision:** Admin always supplies an initial password directly; invite-based provisioning is not implemented in M3.

**Database:** `student`, `teacher`, `parent`, `admin`, `parent_student_link` tables matching `Database_Design.md` §6.2–6.6 column-for-column. Alembic revision `0004_role_profiles` (down_revision `0003`). Learning directly from the Milestone 2 review's autogenerate-index finding: `index=True`/`UniqueConstraint` were declared on the models themselves from the start (not just in the migration) — verified with a fresh `alembic revision --autogenerate` producing a genuinely empty migration on the first attempt, no follow-up fix needed this time.

**Backend:** `user_repository.py` extended (not a new repository file, per its own M2 docstring and the roadmap's file list) with student/teacher/parent/admin queries. `user_service.py`: VR-009 enforced structurally (`MeUpdate` has no `role`/`is_active`/`department_id` field for a client to even attempt to set, not just a runtime rejection); BR-006 deactivation as an idempotent `is_active` flag, never a row deletion; `POST /users/students`/`/teachers` create the `user` and profile rows in one atomic transaction so a duplicate-email failure never orphans either row. `users.py` router: `GET`/`PUT /users/me` (any authenticated role, ownership implicit from the token — no `{id}` param exists to check); `/users/students` (Admin+Teacher read, Admin write, matching `API_Contract.md` §2.3 exactly); `/users/teachers` (Admin-only for both read and write, narrower than `/users/students` per the contract). `seed_admin.py`: bootstraps the first Admin from process-level env vars, idempotent, verified via the bare `python -m scripts.seed_admin` invocation (not just `-m` from a different cwd) against a disposable database.

**Real, pre-existing defect found and fixed during this milestone's own review (not introduced by M3, but found here):** `backend/pyproject.toml` still listed `passlib[bcrypt]` and was missing `email-validator`, having drifted out of sync with `requirements.txt` since Milestone 2's passlib removal, despite `pyproject.toml`'s own header comment stating it's "kept in sync." Corrected in the same commit as the M3 work that surfaced it.

**Tests:** 49 new tests — unit tests for `UserService` (repository stubbed, no DB) covering VR-009's schema-level field exclusion, BR-006 idempotency and row-preservation, department-existence validation, and email-uniqueness/atomicity; integration tests for the `users` router (RBAC 401/403 across both `/users/students`' Admin+Teacher and `/users/teachers`' Admin-only access, full create→list→get→update→deactivate→reactivate lifecycle, VR-009's "extra fields are inert" behavior end-to-end) against a disposable Postgres database. Full suite: **72 passed** (0 failed).

**Verified (not just claimed):** `pytest` 72/72 against a disposable `ict_education_m3_final_verify` database, created and dropped solely for this run. `alembic upgrade head` / `downgrade -1` / `upgrade head` all clean via the bare `alembic` console script. `alembic revision --autogenerate` produces an empty migration (temporary diff-check file deleted immediately after inspection). `npx tsc --noEmit` and `npm run build` both clean (frontend). Backend boots and serves real HTTP (`GET /health` → `200 {"status":"ok","database":"ok"}`); a full live smoke test via `curl` against a disposable database confirmed: admin login, `GET /users/me`, department creation, student creation via `POST /users/students`, the newly created student's own login, that student's `GET /users/me`, and a `403` rejection when that student attempted `POST /users/students`. All disposable databases (`ict_education_m3_verify`, `ict_education_m3_final_verify`) dropped after use; the developer's real `university_management_db` and both `backend/.env`/`frontend/.env` files were never touched — hashed before this milestone began and confirmed byte-identical after every phase (see the `.env` verification note below).

**`.env` verification (explicit, per this milestone's environment-safety instructions):** `md5sum` of both `backend/.env` and `frontend/.env` was recorded before any work began and re-checked after every phase (backend implementation, frontend implementation, and final verification). Both hashes were identical at every checkpoint: `backend/.env` = `bea10a0418e41415c9b0fa839b698653`, `frontend/.env` = `b4f0f938692ebbb360344f61b70713ec`. Per the same instructions, the preview tooling was not used at all this milestone (it was the subject of the prior milestone's `.env`-drift audit) — all verification used process-level env vars on direct `Bash`/`alembic`/`uvicorn` invocations and disposable databases only.

**Known issues / gaps (tracked, not oversights):**
1. **Frontend UI not visually exercised in a browser.** Per this milestone's explicit instruction not to rely on preview tooling, verification was limited to `tsc --noEmit`, `npm run build`, and manual code review — the Profile and Admin: User Management pages have not been seen rendering or clicked through. Recommend a manual browser check (via a mechanism the user trusts, e.g. their own local `npm run dev`) before treating the frontend as fully verified.
2. **FR-008** ("Student views academic history alongside profile data") is not deliverable by `GET /users/me` as actually contracted (`API_Contract.md` §2.1's response has no academic-history fields, and `enrollment`/`result` don't exist as tables until M4/M6/M7) — left `Pending` in the Requirement Traceability Matrix rather than marked complete, with an explanatory note.
3. Migration `0004_role_profiles` (like `0002`/`0003` before it) is hand-authored, not `alembic revision --autogenerate`'d, though its upgrade/downgrade cycle and an autogenerate diff-check are both confirmed clean against a real database.
4. Parent account creation/linking and invite-based provisioning remain explicitly out of scope, per the two decisions above — not deferred silently, but genuinely unimplemented until a later milestone (or a proposal-ambiguity resolution) revisits them.

**Commits (`b26024f`..`5fe89cb`):** `81dda51` (backend: models, schemas, repository, service, router, migration, seed script, `pyproject.toml` fix, roadmap corrections), `5fe89cb` (frontend: Profile page, Admin: User Management page, `features/users`/`features/departments` hooks, routing, nav).

**Full documentation self-review run** (`CLAUDE.md` §14 item 12, `MILESTONE_VERIFICATION_CHECKLIST.md`): `Database_Design.md` required no changes (schema matched exactly). `API_Contract.md` required no changes (implementation matches the contract exactly, including the narrower `/users/teachers` Admin-only access vs. `/users/students`' Admin+Teacher access). `Requirement_Traceability_Matrix.md` FR-006–FR-016 updated (FR-008 left honestly `Pending` — see Known Issues). `Proposal_vs_Engineering_Additions.md` updated with the `features/departments/index.ts` Derived addition. `Implementation_Roadmap.md` corrected in the same commit that revealed each inconsistency, not backfilled. No undocumented additions found beyond what's logged above.

**Outstanding before M4 can start:** explicit approval of M3. **Approved by user, git tag `v0.4-milestone3`.**

### M4 — Scheduling & Timetable (Completed — 2026-07-06)

Implemented exactly the scope `Implementation_Roadmap.md` defines for Milestone 4: `ClassSession`, `Enrollment`, `ScheduleEntry`, `ScheduleChangeRequest`, and conflict detection. Nothing from Milestone 5 (Attendance) or later was implemented — verified by inspecting every M5+ file before starting and confirming all were still M0's empty placeholders.

**Pre-implementation review, before any code was written:** read all ten governing documents and the full codebase, then reported findings per the user's explicit instruction to stop before coding.
1. **Systemic documentation bug found and fixed roadmap-wide:** every milestone's migration filename was off-by-one relative to the actual sequential numbering Alembic requires, because M0's baseline migration consumes revision `0001` and the roadmap's per-milestone entries never accounted for that. This had already been patched reactively for M2 and M3 individually, but M1's own entry was still wrong, and M4–M9 all had the same bug. Fixed all of them in one pass (`0002` through `0010`) rather than continuing to discover this milestone-by-milestone.
2. **`Implementation_Roadmap.md`'s M4 API list was incomplete:** it omitted `POST /schedule/change-requests` and `POST /schedule/change-requests/{id}/resolve`, even though `schedule_change_request.py` was already in M4's file list and `API_Contract.md` §7.6-7.7 already stated both endpoints are "implemented per Implementation_Roadmap.md Milestone 4." Added to the roadmap's API list.
3. **`Requirement_Traceability_Matrix.md` misattributed those same two endpoints to Milestone 9**, grouped together with the unrelated notification-endpoint gaps — M9's own roadmap section never mentions schedule-change-requests. Corrected to M4.
4. **Two genuine, load-bearing proposal gaps required a stop-and-ask before coding:** `class_session` and `enrollment` are both required tables with no creation endpoint anywhere in the documentation, and — unlike Milestone 3's deferrable Parent-linking gap — `POST /schedule` (M4's own core deliverable) cannot be exercised without them, and M5/M6 both explicitly depend on `class_session` existing. Presented two options via `AskUserQuestion`; the user selected adding minimal Admin-only creation endpoints (`POST /schedule/class-sessions`, `POST /schedule/enrollments`), classified Derived and documented in `API_Contract.md` §7.8-7.9, `Proposal_vs_Engineering_Additions.md`, `Requirement_Traceability_Matrix.md`, and `Implementation_Roadmap.md`.

**Database:** `class_session`, `enrollment`, `schedule_entry`, `schedule_change_request` tables matching `Database_Design.md` §6.9-6.13 column-for-column. Alembic revision `0005_scheduling` (down_revision `0004`). Applying the Milestone 2 review's lesson proactively this time: `index=True`/`UniqueConstraint`/`Index` were declared on the models from the start — `alembic revision --autogenerate` produced an empty diff on the very first attempt, no follow-up fix needed.

**Backend:** `schedule_repository.py` (new) — all SQLAlchemy queries for the four tables, including `find_overlapping_entries()`, a genuine interval-overlap query (`existing.start < new.end AND new.start < existing.end`), not just the DB-level exact-duplicate `UniqueConstraint` that `Database_Design.md` §9 calls for — two bookings with different `start_time` values can still overlap, and BR-005 must catch that case too. `schedule_service.py` (new): VR-007 (start before end) enforced on both create and update; BR-005 conflict checking on create/update (excluding the entry itself on update); BR-004 ownership check on change-request creation (a Teacher may only request a change to a `schedule_entry` whose `teacher_id` matches their own profile — verified via `System_Architecture.md` §6's ownership-check pattern); `GET /schedule/conflicts` computes all overlapping pairs across the current schedule state by day/room/teacher; approving a change request re-validates VR-007/BR-005 against the requested new time before applying it. `schedule.py` router: `GET /schedule/me` (Student+Teacher, scoped via `enrollment`/`teacher_id` respectively — no ownership check needed beyond that, since there's no `{id}` path param); `POST`/`PUT`/`DELETE /schedule` and `GET /schedule/conflicts` (Admin only); the Teacher-submit/Admin-resolve change-request pair; the two Derived endpoints (Admin only).

**Tests:** 27 new tests — unit tests for `ScheduleService` (repositories stubbed) covering VR-007, BR-005 (including the overlapping-but-different-start-time case explicitly), BR-004, and the resolve workflow's idempotency/approve/reject branches; integration tests for the `schedule` router (RBAC across all 9 endpoints, the Derived class-session/enrollment flow, a genuine 409 conflict, VR-007's 422, `GET /schedule/me` for both roles, `DELETE` idempotency, and the full change-request submit→approve workflow with the underlying entry's time verified updated) against a disposable Postgres database. Full suite: **99 passed** (0 failed). One test-fixture bug found and fixed during this milestone: `make_department()`'s fixed default name/code collided when a single test called two fixtures that each independently created a department (same class of bug as a similar one found in Milestone 3) — fixed by making the fixture generate a unique department by default; no application code was affected.

**Frontend:** `features/schedule/index.ts` (React Query hooks for all schedule endpoints); `pages/Timetable/index.tsx`, role-branched per `UI_Wireframes.md` §9's Role Visibility note — Student/Teacher get a read-only weekly grid from `GET /schedule/me` with Teacher-only "Request Change" per cell; Admin gets a schedule-management panel (create class session, enroll student, create entry, check conflicts) since no dedicated Admin screen is named in `Requirement_Analysis.md` §7.

**Verified (not just claimed):** `pytest` 99/99 against a disposable `ict_education_m4_test`/`ict_education_m4_final_verify` database, created and dropped solely for these runs. `pip check` — no broken requirements. `alembic upgrade head` / `downgrade -1` / `upgrade head` all clean via the bare `alembic` console script. `alembic revision --autogenerate` produces an empty migration on the first attempt (temporary diff-check file deleted immediately). `npx tsc --noEmit` and `npm run build` both clean. Backend boots and serves real HTTP; a full live smoke test via `curl` against a disposable database confirmed the entire scheduling workflow end-to-end: department/course/room/semester/teacher/student setup → class-session creation → enrollment → schedule-entry creation → a genuine overlapping-but-different-start-time conflict correctly rejected (409) → `GET /schedule/me` for both Student and Teacher → `GET /schedule/conflicts` → a 403 RBAC rejection for a student attempting `POST /schedule` → the full change-request submit→approve workflow with the entry's time actually updated afterward. A final verification pass also re-confirmed authentication (`GET /users/me`) and existing M1/M3 endpoints (`GET /departments`, `GET /users/students`) still work unchanged alongside the new M4 endpoints. All disposable databases dropped after use; the developer's real `university_management_db` was never touched. `backend/.env`, `frontend/.env`, and `backend/.env.local` hashes were recorded before this milestone began and re-checked after every phase — unchanged throughout (`frontend/.env.local` does not exist, N/A). No preview tooling was used this milestone, per this session's explicit environment-safety instructions.

**Known issues / gaps (tracked, not oversights):**
1. **Admin panel's `class_session_id` fields are raw UUID text inputs, not dropdowns.** No `GET /schedule/class-sessions` list endpoint exists — the Derived endpoints approved for this milestone were deliberately scoped to create-only, matching Milestone 1's reference-data-CRUD precedent of adding exactly what's needed and no more. A future milestone could add a list endpoint if the UX gap becomes a real problem.
2. **FR-051** (instant notifications on schedule change) is not implemented — the `notification` table and dispatch mechanism don't exist until Milestone 9. Left `Pending` in the Requirement Traceability Matrix, same pattern as Milestone 3's FR-008.
3. Migration `0005_scheduling` is hand-authored, not `alembic revision --autogenerate`'d, though its upgrade/downgrade cycle and an autogenerate diff-check are both confirmed clean.
4. Frontend UI not visually exercised in a browser this milestone (same standing limitation as Milestone 3, per the instruction not to rely on preview tooling) — verification was `tsc`/`npm run build`/code review only.

**Commits (`5fe89cb`..`1ddaaf5`):** `b33e4fe` (pre-implementation doc corrections and Derived endpoint scoping), `21a36e8` (backend: models, migration, repository, service, router), `33f5c66` (frontend: Timetable page, `features/schedule`), `1ddaaf5` (backend test suite).

**Full documentation self-review run** (`CLAUDE.md` §14 item 12, `MILESTONE_VERIFICATION_CHECKLIST.md`): `Database_Design.md` required no changes (schema matched exactly). `API_Contract.md` updated in the same commit as the design-gap resolution, not backfilled. `Requirement_Traceability_Matrix.md` FR-045–FR-051 and NFR-015 updated (FR-051 left honestly `Pending`). `Proposal_vs_Engineering_Additions.md` updated with the two Derived endpoints. `Implementation_Roadmap.md` corrected in the same commit that revealed each inconsistency. No undocumented additions found beyond what's logged above.

**Outstanding before M5 can start:** explicit approval of M4. **Approved by user, git tag `v0.5-milestone4`.**

### M5 — Attendance (Completed — 2026-07-07)

Implemented exactly the scope `Implementation_Roadmap.md` defines for Milestone 5: attendance marking, correction, per-student summaries, and the low-attendance warning trigger. Nothing from Milestone 6 (Exams) or later was implemented — verified by inspecting every M6+ file before starting and confirming all were still M0's empty placeholders.

**Pre-implementation review, before any code was written:**
1. **Documentation corrections:** narrowed an overly broad RTM note claiming Parent-facing attendance access "reuses Student-facing endpoints" — FR-032 actually reuses the Teacher/Admin class-based endpoint (`GET /attendance/{classId}`, parent-scoped), not `GET /attendance/me`; the pattern genuinely differs per domain (results/fees do reuse their `/me` endpoints).
2. **BR-008's threshold resolved (not asked, resolved from existing evidence):** the proposal never states a number, but `UI_Wireframes.md` shows "Below 80%" in two independent mockups (Attendance page badge, Notifications panel example) — treated as the de facto documented value, recorded back into `Requirement_Analysis.md` §14 item 4.
3. **One genuine, load-bearing gap required a stop-and-ask:** `UI_Wireframes.md` §15 (Teacher: Attendance Marker) requires a pre-populated roster of enrolled students before marking, but no endpoint anywhere returned "students enrolled in a class session" — `GET /attendance/{classId}` only returns records that already exist, useless for an unmarked date. Presented two options via `AskUserQuestion`; the user selected adding a minimal Derived endpoint, `GET /schedule/class-sessions/{class_session_id}/roster` (implemented via the schedule domain, not attendance, since it operates on `enrollment`/`class_session`).

**Attendance architecture:** all 10 mandatory Attendance Domain Rules from this milestone's kickoff instructions are enforced explicitly in `attendance_service.py`, before any database write, in this order: (2) class session exists — "active" interpreted as "exists" since `class_session` has no status column in `Database_Design.md` §6.9; (7) Teacher must be assigned to the class session (ownership, checked before the schedule-entry existence check so an unauthorized Teacher gets a clean 403 rather than leaking schedule-entry existence details); (3) a schedule entry exists for the class session; VR-005's no-future-dating check; then per-record: (1) student exists and is active (via `user.is_active`, not a `student`-table column); (4) a valid `Enrollment` exists; (5) no duplicate record for student/class_session/date (pre-flight check plus an `IntegrityError` backstop for races, per `CLAUDE.md` §10's "do not rely solely on database constraints"); (6) unrelated students/sessions are rejected as a structural consequence of Rules 1+4, not a separate check; (10) every record in a batch is validated before any is created — one invalid row rejects the whole batch. Rule 8 (Students cannot mark/modify attendance) is enforced at the router's RBAC dependency, never reachable in the service. Rule 9 (Parent access is exactly what's documented) is implemented as: `GET /attendance/{classId}` requires an explicit `student_id` query param for Parent callers and a real `ParentStudentLink` row — no other attendance endpoint grants Parent access at all.

**Database:** `attendance_record` matching `Database_Design.md` §6.22 column-for-column — deliberately no `created_at`/`updated_at` (the schema doesn't call for them, same pattern as `room`/`parent_student_link`; corrections mutate `status` in place). Alembic revision `0006_attendance` (down_revision `0005`). `UniqueConstraint`/`Index` declared on the model itself — `alembic revision --autogenerate` produced an empty diff on the first attempt.

**Backend:** `attendance_repository.py` (new), `schedule_repository.py` extended with `class_session_has_schedule_entry()` and `list_enrolled_students()`, `user_repository.py` extended with `parent_has_linked_student()` (BR-007/NFR-003). `attendance_service.py`: percentage computed on every request per NFR-016 (never cached/stored), with `present`+`late` counted and `excused` excluded from the denominator — a documented engineering assumption (no wireframe evidence for this one, unlike BR-008; recorded in `Requirement_Analysis.md` §14 item 17). `attendance.py` router: `GET /attendance/reports` deliberately registered before `GET /attendance/{class_id}` to avoid FastAPI capturing the literal `/reports` path as a `{class_id}` UUID parameter (a real routing bug caught during implementation, not left in).

**Real gap found and fixed during frontend implementation (not deferred):** `GET /attendance/{classId}`'s documented response shape (`API_Contract.md` §4.3) returned `student_id`/`date`/`status` only — no record `id` — so the Teacher: Attendance Marker's correction workflow (`PUT /attendance/{id}`) had no way to resolve which record to correct purely from that response. Added `id` to the response schema and updated `API_Contract.md` in the same change, per `CLAUDE.md` §9.

**Frontend:** `features/attendance/index.ts`, `features/schedule/index.ts` (added `useClassSessionRoster`). `pages/Attendance/index.tsx`: percentage bar with the low-attendance warning badge, filters, records table — Calendar view toggle present but not implemented (Table view is fully functional; documented known limitation). `pages/Teacher/AttendanceMarker/index.tsx`: Class/Date selectors (Class list reuses `GET /schedule/me` since no class-session list endpoint exists, per Milestone 4's deliberate create-only scope), roster table, "Mark all present," and Save — detecting correction mode (pre-filled, `PUT` instead of `POST`) when the selected date already has records, now fully wired up after the `id`-field fix above.

**Tests:** 35 new tests — unit tests for `AttendanceService` (repositories stubbed) covering every one of the 10 mandatory rules individually plus the percentage/warning computation; integration tests for the `attendance` router and the roster endpoint (RBAC across all 6 endpoints, duplicate-marking 409, unenrolled-student 422, wrong-teacher 403, missing-schedule-entry 422, correction via PUT, Parent access with/without a valid link, Admin reports) against a disposable Postgres database. Full suite: **134 passed** (0 failed).

**Verified (not just claimed):** `pytest` 134/134 against a disposable `ict_education_m5_test`/`ict_education_m5_final_verify` database, created and dropped solely for these runs. `pip check` clean. `alembic upgrade head`/`downgrade -1`/`upgrade head` all clean via the bare `alembic` console script. `alembic revision --autogenerate` empty diff on the first attempt. `npx tsc --noEmit` and `npm run build` both clean. Backend boots and serves real HTTP; a full live smoke test via `curl` against a disposable database confirmed the entire attendance workflow end-to-end: roster listing, attendance marking, duplicate rejection (409), student-role rejection (403) on marking, `GET /attendance/me` with correct percentage/warning computation, `PUT` correction flipping the warning flag, `GET /attendance/{classId}`, `GET /attendance/reports`, and unauthenticated 401. A dedicated regression pass (per this milestone's explicit Cross-Milestone Regression Protection requirement) re-confirmed, on a freshly-migrated disposable database, that authentication, `GET /departments` (M1), `GET /users/students` (M3), and `GET /schedule/conflicts` (M4) all still work unchanged alongside the new M5 endpoints. All disposable databases dropped after use; the developer's real `university_management_db` was never touched. `backend/.env`, `frontend/.env`, `backend/.env.local` SHA-256 hashes were recorded before this milestone began and re-checked after every phase — unchanged throughout (`frontend/.env.local` does not exist). No preview tooling was used this milestone.

**Known issues / gaps (tracked, not oversights):**
1. **Calendar view** on the Student Attendance page is not implemented — Table view is fully functional; the toggle button exists but Calendar mode shows a placeholder message.
2. **FR-031/FR-032's notification-dispatch half** (instant warnings/absence alerts) is not implemented — the `notification` module doesn't exist until Milestone 9. The computed `low_attendance_warning` indicator itself is fully implemented and tested.
3. **Attendance percentage formula** (`present`+`late` counted, `excused` excluded) is a documented engineering assumption with no proposal/wireframe evidence, unlike BR-008's threshold — flagged for revisit if the institution's actual policy is ever specified.
4. Migration `0006_attendance` is hand-authored, not `alembic revision --autogenerate`'d, though its upgrade/downgrade cycle and an autogenerate diff-check are both confirmed clean.
5. Frontend UI not visually exercised in a browser this milestone (same standing limitation as M3/M4, per the instruction not to rely on preview tooling) — verification was `tsc`/`npm run build`/code review only.

**Commits (`1ddaaf5`..`611281c`):** `41fdda0` (pre-implementation doc corrections and roster endpoint scoping), `b98d52c` (backend: models, migration, repository, service, router), `e1127ea` (frontend + the `ClassAttendanceEntry.id` schema fix), `611281c` (backend test suite).

**Full documentation self-review run** (`CLAUDE.md` §14 item 12, `MILESTONE_VERIFICATION_CHECKLIST.md`): `Database_Design.md` required no changes. `API_Contract.md` updated twice in the same change as each gap that revealed it (the roster endpoint pre-implementation, the `id` field during frontend work) — not backfilled. `Requirement_Traceability_Matrix.md` FR-026–FR-032, NFR-002/NFR-003/NFR-016 updated, with FR-031/FR-032/NFR-002/NFR-003/NFR-016 honestly marked partially-implemented where a later milestone owns the remainder. `Proposal_vs_Engineering_Additions.md` updated with the roster endpoint's Derived classification. `Requirement_Analysis.md` §14 gained two new resolved items (17 for the percentage formula; item 4 updated for the threshold). `Implementation_Roadmap.md` corrected in the same commit that revealed each inconsistency. No undocumented additions found beyond what's logged above.

**Outstanding before M6 can start:** explicit approval of M5. **Approved by user, git tag `v0.6-milestone5`.**

### M6 — Exams & Grading (Completed — 2026-07-15)

Implemented exactly the scope `Implementation_Roadmap.md` defines for Milestone 6: exam creation/editing, the timed exam-taking flow, submission, and grading. Nothing from Milestone 7 (Results & Transcripts) was implemented — per the kickoff's explicit instruction, no final course results, GPA, or transcript aggregation was calculated or persisted; `exam.status = published` (BR-001/FR-025) is an exam-level fact only, independent of M7's `result` table.

**Pre-implementation review, before any code was written:** read all governing documents and the full codebase, then reported findings per the user's explicit instruction to stop before coding.
1. **One genuine, load-bearing gap required a stop-and-ask before coding:** `UI_Wireframes.md` §5 (Exam Room) describes zero server round-trips during an exam attempt except the final submit (answers autosave client-side only), but nothing anywhere recorded a server-side exam-start time, and VR-004's time-limit enforcement cannot be genuine without one — a client-supplied elapsed time could be falsified. Presented via `AskUserQuestion`; the user approved the Derived Engineering Addition `POST /exams/{id}/start` with explicit requirements (create-if-none, server-clock-only, idempotent, immutable `started_at`, all RBAC/ownership/business-rule validation before write). Documented in `API_Contract.md` §3.6, `Implementation_Roadmap.md`, `Requirement_Traceability_Matrix.md`, and `Proposal_vs_Engineering_Additions.md` before implementation.
2. **`Requirement_Traceability_Matrix.md`'s FR-025 row was factually wrong:** it mapped to `GET /exams/{id}/results (student-scoped)` and referenced the Milestone 7 `result` table, but `API_Contract.md` restricts that endpoint to Teacher/Admin only. Corrected to `GET /exams/{id}` (BR-001's actual Student-facing gate), clarifying that "published" means `exam.status = published`, an exam-level fact distinct from M7's per-student `result` table.
3. **Two ambiguities resolved from existing textual evidence, not asked:** the exam-status-transition mechanism (`UI_Wireframes.md` §13's "Publish Exam (transitions status...)" wording resolves to `PUT /exams/{id}`'s optional `status` field, not a separate endpoint) and the grading re-save policy (`API_Contract.md`'s own flagged "policy TBD," resolved by §14's "Save Grades" wording to an upsert-per-answer, re-saveable action).
4. **A second Derived gap found during frontend implementation:** `POST /exams/{id}/grade` requires `answer_id` values, but no endpoint returned a submission's actual answer content (`answer_text`/`selected_option_id`) alongside them, so the documented Grading Interface (`UI_Wireframes.md` §14) could not be built. Presented via `AskUserQuestion`; the user approved `GET /exams/{id}/submissions/{submission_id}` (Teacher-creator-or-Admin only), documented in the same four files before implementation.

**Examination architecture:** all 12 mandatory Examination Domain Rules from this milestone's kickoff instructions are enforced explicitly in `exam_service.py`/`grading_service.py`, before any database write: exam/class-session/student existence checks return 404/422 as appropriate; only the class session's assigned Teacher may create an exam for it (ownership, Domain Rule 6); Students cannot create/modify/grade (enforced at router RBAC, never reachable in the service); enrollment is required to start/submit (Domain Rule 5); `exam_submission`'s own uniqueness constraint plus a pre-flight check prevent a second attempt once one is `submitted`/`graded`; every MCQ question must have at least one correct option; VR-006 validates the whole grading batch before any write; all validation happens before any write in both `submit_exam` (all answers' `question_id`s validated before any `Answer` is created) and `grade_submission` (all grades' `awarded_marks` validated against `question.marks` before any `question_grade` is written).

**BR-001 masking:** `ExamService.get_exam` computes `reveal_to_student = current_user.role != "student" or exam.status == "published"` and conditionally nulls `QuestionOptionRead.is_correct`/`QuestionRead.awarded_marks`/`QuestionRead.feedback` — the same response schema serves all roles/statuses, never a separate response shape per BR-001 masking pattern established in prior milestones.

**BR-003 immutability:** `update_exam`/`delete_exam` both reject with 409 once `exam.status == "published"`; status transitions via `PUT`'s optional `status` field are forward-only (`EXAM_STATUS_ORDER.index(new) >= EXAM_STATUS_ORDER.index(current)`), rejecting any backward move with 422.

**Database:** `exam`, `question`, `question_option`, `exam_submission`, `answer`, `question_grade` tables matching `Database_Design.md` §6.14-6.19 column-for-column, including the deliberate `ON DELETE` policy split (§10): `question`/`question_option` cascade from their unpublished-only parents, but `answer.submission_id`/`answer.question_id` stay `RESTRICT` (a real student's answer is never silently destroyed by an exam-content edit). Alembic revision `0007_exams` (down_revision `0006`). `index=True`/`UniqueConstraint`/`CheckConstraint` declared on the models themselves — `alembic revision --autogenerate` produced an empty diff on the first attempt.

**Backend:** `exam_repository.py` (new) — all SQLAlchemy queries across the six exam-domain tables. `exam_service.py`/`grading_service.py` (new): business rules and workflow above. `exams.py` router: 9 endpoints (`GET/POST /exams`, `GET/PUT/DELETE /exams/{id}`, `POST /exams/{id}/start`, `POST /exams/{id}/submit`, `GET /exams/{id}/submissions/{submission_id}`, `POST /exams/{id}/grade`, `GET /exams/{id}/results`) — no path-ordering issues since no sub-path collides with a literal segment (unlike Milestone 5's `/reports` vs. `/{class_id}` bug).

**Frontend:** `features/exams/index.ts` (React Query hooks for all 9 endpoints). `pages/ExamList/index.tsx`: role-scoped table per `UI_Wireframes.md` §4 (Class/Status filters, row-click routing to Exam Room/Exam Builder/Grading Interface depending on role and status; draft exams hidden from Students server-side, not just client-side). `pages/ExamRoom/index.tsx`: calls `POST /exams/{id}/start` on entry, countdown derived from the server-recorded `started_at` (never a client clock, so a page refresh cannot extend the time limit), question navigator, MCQ/free-text inputs, confirmation-gated submit, timer-expiry auto-submit. `pages/Teacher/ExamBuilder/index.tsx`: dynamic question/option editor, Save Draft (keeps current status) vs. Publish Exam (`status: "open"`, making the exam available to students — a status-transition mapping decision recorded in the page's own docstring), blocked once published (BR-003). `pages/Teacher/GradingInterface/index.tsx`: submission selector backed by the new submission-detail endpoint, per-question marks/feedback inputs, Save Grades (re-saveable), and a Publish Exam action (`status: "published"`) once every submission is graded — "Submit Results for Approval" (`UI_Wireframes.md` §14) is explicitly Milestone 7 scope and was not implemented.

**Tests:** 77 new tests — 50 unit tests (`ExamService`/`GradingService`, repositories stubbed) covering every mandatory Examination Domain Rule, BR-001/BR-003, VR-004/VR-006, and both Derived endpoints' idempotency/ownership rules individually; 27 integration tests (full request → DB → response cycle against a disposable database) covering RBAC across all 9 endpoints, the full exam lifecycle (create → publish → start → submit → grade → results), VR-004 (time limit exceeded, verified by backdating `started_at` directly in the test database — a real multi-minute sleep isn't practical in a test suite), VR-006 rejection, re-grading, and the two Derived endpoints' negative/ownership cases. Full suite: **211 passed** (0 failed) — a full regression run across Milestones 1-6.

**Verified (not just claimed):** `pytest` 211/211 against a disposable `ict_education_m6_test` database, created and dropped solely for this run. `pip check` clean. `alembic upgrade head`/`downgrade base`/`upgrade head` all clean via the bare `alembic` console-script entry point. `alembic revision --autogenerate` produced an empty migration on the first attempt (temporary diff-check file deleted immediately after inspection). `npx tsc --noEmit` and `npm run build` both clean. Backend boots and serves real HTTP via a live `uvicorn` process (not just `TestClient`): `GET /health` → `200 {"status":"ok","database":"ok"}`, a wrong-password login correctly returns `401` with the standard error envelope. All disposable databases dropped after use (confirmed via `pg_database` query — none remain); the developer's real `university_management_db` was never touched. `backend/.env`, `frontend/.env`, `backend/.env.local` SHA-256 hashes were recorded before this milestone began and re-checked after every phase (backend implementation, Derived-endpoint implementation, frontend implementation, full test suite/migration verification) — unchanged throughout (`frontend/.env.local` does not exist). No preview tooling was used this milestone, per the standing environment-safety instructions.

**Known issues / gaps (tracked, not oversights):**
1. Migration `0007_exams` is hand-authored, not `alembic revision --autogenerate`'d, though its upgrade/downgrade cycle and an autogenerate diff-check are both confirmed clean.
2. Frontend UI not visually exercised in a browser this milestone (same standing limitation as M3-M5, per the instruction not to rely on preview tooling) — verification was `tsc`/`npm run build`/code review only.
3. Milestone 6 deliberately does not calculate or persist final course results (GPA, transcripts, aggregated per-course marks) — explicitly Milestone 7 scope, per this milestone's own kickoff instructions.

**Commits (`611281c`..`0c3fe44`):** `cc8094b` (pre-implementation doc corrections and exam-start endpoint scoping), `fb62840` (backend: models, migration, repository, services, router), `b6203e4` (docs: submission-detail Derived addition), `b416f31` (backend: submission-detail endpoint), `9743c0a` (frontend: exam builder, exam room, grading, routing/nav), `0c3fe44` (backend test suite).

**Full documentation self-review run** (`CLAUDE.md` §14 item 12, `MILESTONE_VERIFICATION_CHECKLIST.md`): `Database_Design.md` required no changes (schema matched exactly). `API_Contract.md` updated twice in the same change as each Derived addition that revealed it (§3.6 pre-implementation, §3.8 during frontend work) — not backfilled. `Requirement_Traceability_Matrix.md` FR-017-FR-025 updated to Verified, with the FR-025 endpoint/table mapping correction recorded as its own note. `Proposal_vs_Engineering_Additions.md` updated with both Derived endpoints' classifications. `Implementation_Roadmap.md` gained two scope notes, each added in the same change as the addition it documents. No undocumented additions found beyond what's logged above.

**Outstanding before M7 can start:** explicit approval of M6. **Approved by user, git tag `v0.7-milestone6`.**

### M7 — Results & Transcripts (Completed — 2026-07-17)

Implemented exactly the scope `Implementation_Roadmap.md` defines for Milestone 7: the result submission → approval → publication workflow and PDF transcript generation. Nothing from Milestone 8 (Fees) was implemented.

**Pre-implementation review, before any code was written:** read all governing documents and the full codebase, then reported findings per the user's explicit instruction to stop before coding.
1. **One genuine, load-bearing schema gap required a stop-and-ask before coding:** `Database_Design.md` §6.21's original `result` table had no `exam_id`, but `POST /results/{examId}/submit` submits per-exam and the Admin: Result Approval wireframe groups its queue by Exam name — neither the queue display nor Milestone 7's mandatory Domain Rule 6 (QuestionGrade-to-Result traceability) could be satisfied without it, and a course with more than one exam per semester would hit the `(student_id, course_id, semester_id)` unique constraint on a second submission. Presented via `AskUserQuestion`; the user approved adding a nullable `exam_id` FK, with `(student_id, course_id, semester_id)` remaining the authoritative business key.
2. **Resubmission-after-reject policy, confirmed with the user in the same question round:** a `rejected` result may be resubmitted (updated in place) via `POST /results/{examId}/submit`; a `submitted`/`published` row still blocks a duplicate with 409 — otherwise the reject workflow would be a dead end.
3. **A second genuine gap found during frontend implementation:** the Admin: Result Approval queue (`UI_Wireframes.md` §11) needs a list of pending results grouped by exam, but none of the milestone's other four documented endpoints can list/retrieve pending results, and `GET /results/reports` is a different, aggregate/published-only reporting endpoint out of scope. Presented via `AskUserQuestion`; the user approved `GET /results/pending` (Derived Engineering Addition, Admin-only).
4. **Resolved without needing to ask, from existing documented evidence:** GPA formula (credit-hour-weighted average per semester, per `Requirement_Analysis.md`'s own A-004 assumption, resolving §14 item 6); `GET /results/me` Parent scoping (required `student_id` query param verified against `parent_student_link`, same pattern as Milestone 5's `GET /attendance/{classId}`); `POST /results/{id}/approve`'s reject-comment requirement (mandatory, per `UI_Wireframes.md` §11's own wireframe text, closing the contract's previously-flagged "policy TBD"); `GET /results/{studentId}/transcript` with no published results yet (200 with an empty-results PDF, not 409 — a transcript describes current state, and "no results yet" is a legitimate displayable state).
5. **A third, smaller gap found during frontend implementation:** `GET /results/me`'s documented response had no `student_id`, so the frontend had no way to construct the transcript-download URL for "download my own transcript." Fixed in the same change (same class of fix as Milestone 5's `GET /attendance/{classId}` `id`-field addition) — added `student_id` to the response.

**Results architecture:** all 15 mandatory Results & Academic Records Domain Rules from this milestone's kickoff instructions are enforced explicitly in `result_service.py`, before any database write: student existence/active (Rule 1), enrollment in the exam's class (Rule 2), exam existence (Rule 3), exam published — not just fully graded — before contributing to Results (Rule 4, ties directly into the Milestone 6 Grading Interface's own "Publish Exam" action as the actual gate), exam fully graded across every submission (Rule 5), the target student actually has a graded submission for this exact exam — not just any grade hand-typed for them (Rule 6), duplicate prevention with the resubmission-after-reject exception (Rule 7), GPA/grade calculations following only the documented formula (Rule 8 — see below), read-only access to `exam`/`exam_submission`/`answer`/`question_grade` (Rules 9-10, no method in `result_service.py` ever calls a mutating `ExamRepository` method), RBAC/ownership for Student (own only)/Parent (linked only)/Teacher (own exams only)/Admin (Rules 11-14), and two-pass validate-the-whole-batch-then-write for both submission and grading operations (Rule 15).

**GPA/grade calculation:** `grade_letter`/`grade_point` are never computed from raw exam marks anywhere in this codebase — per `API_Contract.md` §5.2, the submitting Teacher supplies both directly per student, so no letter-grade-boundary formula was invented or hard-coded. The one formula this milestone does implement is semester GPA: a credit-hour-weighted average of `grade_point` across a semester's `published` results (`sum(grade_point * course.credit_hours) / sum(course.credit_hours)`), resolving `Requirement_Analysis.md` §14 item 6 per that document's own A-004 assumption — not a new invention.

**Database:** `result` table matching `Database_Design.md` §6.21 column-for-column, including the Milestone 7 `exam_id` addition (nullable, `ON DELETE RESTRICT`). Alembic revision `0008_results` (down_revision `0007`). `index=True`/`UniqueConstraint` declared on the model itself — `alembic revision --autogenerate` produced an empty diff on the first attempt.

**Backend:** `result_repository.py` (new) — all SQLAlchemy queries for `result`. `result_service.py` (new): business rules and workflow above, reusing `ExamRepository`/`ScheduleRepository`/`UserRepository`/reference-data repositories read-only. `transcript_generator.py` (new): `reportlab`-based PDF generation (added to `requirements.txt`/`pyproject.toml`, finalizing `System_Architecture.md` §12's "library choice TBD"), run via `fastapi.concurrency.run_in_threadpool` to keep the async event loop unblocked while still matching the documented synchronous binary-stream response. `results.py` router: 5 endpoints (`GET /results/me`, `POST /results/{examId}/submit`, `GET /results/pending`, `POST /results/{id}/approve`, `GET /results/{studentId}/transcript`).

**Frontend:** `features/results/index.ts` (React Query hooks for all 5 endpoints, including a transcript-download hook that fetches a blob and triggers a browser download via an object URL). `pages/ResultsView/index.tsx`: semester selector, GPA summary, per-course table, Download Transcript button — Student-facing only this milestone, matching the roadmap's file list; Parent access is fully implemented and tested server-side but has no frontend page yet (see Known Issues). `pages/Admin/ResultApproval/index.tsx`: pending-results queue with a status filter, expandable per-exam review panel, Approve/Reject requiring a comment on reject.

**Tests:** 42 new tests — 25 unit tests (`ResultService`, repositories stubbed) covering every mandatory Domain Rule individually, the GPA formula against a mixed-credit-hours scenario, and the approval/transcript ownership rules; 17 integration tests (full request → DB → response cycle against a disposable database) covering RBAC across all 5 endpoints, the full workflow (exam published → submit results → pending queue → approve/reject → student sees published result → transcript download), the resubmission-after-reject workflow, and Parent-scoped access with/without a valid link. Full suite: **253 passed** (0 failed) — a full regression run across Milestones 1-7.

**Verified (not just claimed):** `pytest` 253/253 against a disposable `ict_education_m7_test` database, created and dropped solely for this run. `pip check` clean. `alembic upgrade head`/`downgrade -1`/`upgrade head` all clean via the bare `alembic` console-script entry point. `alembic revision --autogenerate` produced an empty migration on the first attempt (temporary diff-check file deleted immediately after inspection). `npx tsc --noEmit` and `npm run build` both clean. Backend boots and serves real HTTP via a live `uvicorn` process: `GET /health` → `200 {"status":"ok","database":"ok"}`, wrong-password login → `401` standard envelope, an unauthenticated `GET /results/me` → `401`. A dedicated live smoke test (via `TestClient` against a disposable database, before the formal test suite was written) walked the entire cross-milestone workflow end-to-end — department/course/semester/teacher/student setup (M1/M3) → class session/enrollment (M4) → exam creation/grading/publishing (M6) → results submission → pending queue → approval → published result visible to the student with the correct credit-weighted GPA → transcript PDF (`200`, `application/pdf`, valid `%PDF` bytes) → RBAC rejection. All disposable databases (`ict_education_m7_dev`, `ict_education_m7_test`) dropped after use — confirmed via a `pg_database` query that none remain; the developer's real `university_management_db` was never touched. `backend/.env`, `frontend/.env`, `backend/.env.local` SHA-256 hashes were recorded before this milestone began and re-checked after every phase (pre-implementation, backend implementation, Derived-endpoint implementation, frontend implementation, full test suite/migration verification) — unchanged throughout (`frontend/.env.local` does not exist). No preview tooling was used this milestone.

**Known issues / gaps (tracked, not oversights):**
1. **Parent-facing frontend UI for Results is not built.** The backend fully implements and tests FR-037 (Parent, linked-child-scoped access to `GET /results/me`), but no frontend page consumes it — no endpoint anywhere lists a Parent's linked children (only ownership *verification* via `parent_student_link`, not enumeration), and the full Parent Portal page (`UI_Wireframes.md` Page 17, Parent: Child View) isn't scheduled for this milestone. Deferred to whichever future milestone builds that page, which will need the same "list my children" capability for attendance/fees/schedule too, not just results.
2. **`POST /results/{id}/approve`'s reject `comment` is validated but not persisted.** `Database_Design.md` §6.21's `result` table has no comment/rejection-reason column, and `API_Contract.md`'s documented response shape for this endpoint doesn't echo one back either — this is a pre-existing schema limitation (predates this milestone's `exam_id` addition), not introduced here. The reject workflow functions correctly (comment required, request rejected without one), but the reason text itself is not stored or retrievable later.
3. Migration `0008_results` is hand-authored, not `alembic revision --autogenerate`'d, though its upgrade/downgrade cycle and an autogenerate diff-check are both confirmed clean.
4. Frontend UI not visually exercised in a browser this milestone — per the standing instruction not to rely on preview tooling, verification was `tsc`/`npm run build`/code review only.
5. Milestone 7 does not implement result-publication notifications (FR-052) — the `notification` table/dispatch mechanism doesn't exist until Milestone 9, same pattern as Milestone 4/5's schedule-change/attendance-warning notification gaps.

**Commits (`0c3fe44`..`2af8744`):** `86f78bd` (pre-implementation: `result.exam_id`/resubmission-policy doc resolutions), `68d9672` (docs: `GET /results/pending` Derived addition), `5c1e13c` (backend: models, migration, repository, service, router, PDF generator), `c2241f8` (frontend: Results View, Result Approval, bundled `student_id` response-field fix), `2af8744` (backend test suite).

**Full documentation self-review run** (`CLAUDE.md` §14 item 12, `MILESTONE_VERIFICATION_CHECKLIST.md`): `Database_Design.md` updated in the same change as the `exam_id` design-gap resolution, not backfilled. `API_Contract.md` updated three times, each in the same change as the gap that revealed it (the pre-implementation resolutions, the `GET /results/pending` addition, the `student_id` field fix). `Requirement_Traceability_Matrix.md` FR-033-FR-037 updated to Verified, with FR-037 honestly annotated as backend-only pending a future frontend page. `Proposal_vs_Engineering_Additions.md` updated with both Derived additions (`result.exam_id`, `GET /results/pending`). `Requirement_Analysis.md` §14 item 6 marked resolved. `Implementation_Roadmap.md` gained two scope notes, each added in the same change as the addition it documents. No undocumented additions found beyond what's logged above.

**Outstanding before M8 can start:** explicit approval of M7. **Approved by user, git tag `v0.8-milestone7`.**

### M8 — Fees (Completed — 2026-07-19)

Implemented exactly the scope `Implementation_Roadmap.md` defines for Milestone 8: fee structure definition, payment recording, invoicing, and overdue tracking. Nothing from Milestone 9 (Notifications) was implemented — verified by inspecting `app/models/notification.py` and every other M9+ file before starting and confirming all were still M0's empty placeholders.

**Pre-implementation review, before any code was written:** read all governing documents and the full codebase, then reported findings per the user's explicit instruction to stop before coding.
1. **One genuine, load-bearing gap required a stop-and-ask before coding:** neither `Database_Design.md` nor `API_Contract.md` documented how an `invoice` row actually gets created — `fee_structure` is department/semester-scoped, not student-scoped, and `POST /fees/payments` only recalculates an existing invoice's status. The Fee Centre wireframe shows a nonzero "Outstanding Balance" before any payment exists, meaning an unpaid invoice must already exist independently. Presented via `AskUserQuestion`; the user approved: `POST /fees` (fee_structure creation) auto-generates one `unpaid` invoice for every currently-active student with ≥1 `Enrollment` in a `class_session` for that semester, department-matching (or all, if university-wide) — documented behavior of the existing endpoint, not a new one.
2. **Resolved without needing to ask, from this milestone's own explicit Fees Domain Requirements:** VR-008's overpayment policy (strictly disallowed — closes `API_Contract.md`'s own previously-flagged "unresolved" note); invoice `overdue` status (computed at read time from `due_date`, never stored, same "computed on demand" philosophy as attendance percentage/GPA); `GET /fees/me` Parent scoping (required `student_id` query param, same pattern as Milestone 7's `GET /results/me`); `pdf_url` stays permanently null (invoice PDFs generated on demand via `reportlab`, same as Milestone 7's transcript generation).

**Fees architecture:** all 17 mandatory Fees Domain Rules from this milestone's kickoff instructions are enforced explicitly in `fee_service.py`, before any database write: student existence/active (Rules 1-2), enrollment-gated eligibility for invoice auto-generation (Rule 3), existing-semester requirement on fee structure creation (Rule 4), a defensive duplicate-invoice check even though the `(student_id, fee_structure_id)` unique key makes it structurally unreachable in the normal flow (Rule 5), payment-vs-remaining-outstanding-balance and non-negative-balance enforcement (Rules 6-8), fully-paid-invoice payment rejection (Rule 9), payment immutability — no update/delete endpoint exists at all (Rule 10), RBAC/ownership for Student (own only)/Parent (linked only)/Teacher (no fee endpoints granted at all)/Admin (Rules 11-14), server-side-only financial calculations derived from persisted `payment` rows, never trusted from client input (Rules 15-16), and the invoice-generation batch's all-or-nothing nature — a single eligibility query means no partial-failure state is reachable (Rule 17).

**Database:** `fee_structure`, `invoice`, `payment` tables matching `Database_Design.md` §6.23-6.25 column-for-column, including the Milestone 8 addition of a unique `(student_id, fee_structure_id)` constraint on `invoice`. Alembic revision `0009_fees` (down_revision `0008`). `index=True`/`UniqueConstraint`/`CheckConstraint` declared on the models themselves — `alembic revision --autogenerate` produced an empty diff on the first attempt.

**Backend:** `fee_repository.py` (new) — all SQLAlchemy queries across the three fee-domain tables, including `list_eligible_students()` (joins `Student`/`User`/`Enrollment`/`ClassSession` for invoice auto-generation eligibility). `fee_service.py` (new): business rules and workflow above. `invoice_generator.py` (new): `reportlab`-based PDF generation, run via `fastapi.concurrency.run_in_threadpool`, same pattern as Milestone 7's transcript generator. `fees.py` router: 6 endpoints (`GET /fees/me`, `POST /fees`, `POST /fees/payments`, `GET /fees/payments/{studentId}`, `GET /fees/invoices/{id}`, `GET /fees/overdue`).

**Frontend:** `features/fees/index.ts` (React Query hooks for all 6 endpoints, including an invoice-download hook mirroring Milestone 7's transcript-download blob pattern). `features/semesters/index.ts` (Derived, same precedent as Milestone 3's `features/departments` — thin wrapper around the existing `GET /semesters` endpoint, needed for the Fee Dashboard's Semester selector). `pages/FeeCentre/index.tsx`: outstanding balance card, payment history table, per-invoice download — Student-facing only this milestone, matching the roadmap's file list; Parent access is fully implemented and tested server-side but has no frontend page yet, same known gap as Milestone 7's Results View. `pages/Admin/FeeDashboard/index.tsx`: stat cards, New Fee Structure / Record Payment forms, overdue accounts table — deliberately omits the wireframe's Notify/Bulk-notify actions (FR-056), explicitly deferred to Milestone 9 per the roadmap's own note since that endpoint writes to the `notification` table.

**Tests:** 40 new tests — 24 unit tests (`FeeService`, repositories stubbed) covering every mandatory Domain Rule individually, including a department-scoped eligibility case and the derived-overdue-status-overrides-stored-status case; 16 integration tests (full request → DB → response cycle against a disposable database) covering RBAC across all 6 endpoints, invoice auto-generation (including a department-mismatch case producing zero invoices), the full unpaid→partially_paid→paid payment lifecycle with overpayment/fully-paid rejections, and invoice PDF ownership. Full suite: **293 passed** (0 failed) — a full regression run across Milestones 1-8.

**Verified (not just claimed):** `pytest` 293/293 against a disposable `ict_education_m8_test` database, created and dropped solely for this run. `pip check` clean. `alembic upgrade head`/`downgrade -1`/`upgrade head` all clean via the bare `alembic` console-script entry point. `alembic revision --autogenerate` produced an empty migration on the first attempt (temporary diff-check file deleted immediately after inspection). `npx tsc --noEmit` and `npm run build` both clean. Backend boots and serves real HTTP via a live `uvicorn` process: `GET /health` → `200 {"status":"ok","database":"ok"}`, wrong-password login → `401` standard envelope, an unauthenticated `GET /fees/me` → `401`. A dedicated live smoke test (via `TestClient` against a disposable database, before the formal test suite was written) walked the entire cross-milestone workflow end-to-end: department/course/semester/teacher/student setup (M1/M3) → class session/enrollment (M4) → fee structure creation with automatic invoice generation → unpaid → partial payment → partially_paid → overpayment rejected (409) → full payment → paid → further payment against paid invoice rejected (409) → invoice PDF download → payment history → a separate past-due fee structure correctly appearing in `GET /fees/overdue` with the right `days_overdue` → RBAC rejection. All disposable databases (`ict_education_m8_dev`, `ict_education_m8_test`) dropped after use — confirmed via a `pg_database` query that none remain; the developer's real `university_management_db` was never touched. `backend/.env`, `frontend/.env`, `backend/.env.local` SHA-256 hashes were recorded before this milestone began and re-checked after every phase — unchanged throughout (`frontend/.env.local` does not exist). No preview tooling was used this milestone.

**Known issues / gaps (tracked, not oversights):**
1. Parent-facing frontend UI for Fees is not built — the backend fully implements and tests FR-038/FR-041 Parent access, but no endpoint anywhere enumerates a Parent's linked children, and the full Parent Portal page isn't scheduled for this milestone (same known gap as Milestone 7's Results View).
2. The Admin: Fee Dashboard's "Notify"/"Send Bulk Overdue Notice" actions (FR-056) are not implemented — explicitly deferred to Milestone 9, since `POST /fees/overdue/notify` writes to the `notification` table.
3. The Record Payment form's Fee Structure field is a raw UUID text input, not a dropdown — no `GET /fees` list endpoint exists in the documented API contract (same class of known limitation as Milestone 4's Admin schedule panel).
4. Migration `0009_fees` is hand-authored, not `alembic revision --autogenerate`'d, though its upgrade/downgrade cycle and an autogenerate diff-check are both confirmed clean.
5. Frontend UI not visually exercised in a browser this milestone (same standing limitation as M3-M7) — verification was `tsc`/`npm run build`/code review only.
6. FR-044 (automatic due-date reminders/overdue notices) is not implemented — depends on the Milestone 9 notification module.

**Commits (`2af8744`..`8258ea7`):** `f9a8702` (pre-implementation: invoice-creation and fee-policy resolutions), `3c376f2` (backend: models, migration, repository, service, router, PDF generator), `3a23389` (frontend: fee centre, fee dashboard, routing/nav), `8258ea7` (backend test suite).

**Full documentation self-review run** (`CLAUDE.md` §14 item 12, `MILESTONE_VERIFICATION_CHECKLIST.md`): `Database_Design.md` updated in the same change as the invoice-creation design-gap resolution, not backfilled. `API_Contract.md` updated in the same change as each resolution (invoice auto-generation, overpayment policy, derived overdue, Parent scoping). `Requirement_Traceability_Matrix.md` FR-038-FR-043 updated to Verified; FR-044 left honestly Pending. `Proposal_vs_Engineering_Additions.md` updated with the `features/semesters` Derived addition. `Requirement_Analysis.md` VR-008 marked resolved. `Implementation_Roadmap.md` gained a scope note in the same change as the invoice-creation resolution. No undocumented additions found beyond what's logged above.

**Outstanding before M9 can start:** explicit approval of M8. **Approved by user, git tag `v0.9-milestone8`.**

### M9 — Notifications (Completed — 2026-07-21)

Implemented exactly the scope `Implementation_Roadmap.md` defines for Milestone 9: the cross-cutting notification module (generation, storage, feed UI) and its four automatic dispatch triggers into the domains that already existed. Nothing from Milestone 10 (Dashboards & Reporting) was implemented — confirmed by inspecting `services/report_service.py` (still an untouched placeholder; `routers/reports.py` doesn't exist yet at all, per the roadmap's own M10 file list) and every other M10+ file before starting.

**Pre-implementation review, before any code was written:** read all governing documents and the full codebase, then reported findings per the user's explicit instruction to stop before coding.
1. **One documentation inconsistency fixed as an obvious correction:** `Requirement_Analysis.md` FR-051 and `API_Contract.md` both said schedule-change notifications go to Students only, but `UI_Wireframes.md` §16's own Role Visibility line explicitly lists "schedule changes for Student/Teacher." Corrected in the same change — a rescheduled Teacher is now notified alongside enrolled students.
2. **Two genuine ambiguities required a stop-and-ask before coding, both resolved via `AskUserQuestion`:** (a) attendance-warning notification frequency — the user confirmed threshold-crossing-only semantics (dispatch only when a student's per-class-session percentage goes from `>=80%` to `<80%` as a direct result of the new attendance record; no repeat while chronically below 80%; a new notification if they recover and cross below again later); (b) exam-domain notifications — the kickoff's Domain Rules named "Exams" as a source, but no FR or `notification.type` enum value covers an exam-specific event; the user confirmed no new type/schema change, treating Milestone 7's `result_published` as the sole, sufficient notification for the exam-grading-to-result pipeline.
3. **Resolved without needing to ask:** `fee_due` notifications dispatch at invoice-issuance time (`POST /fees`), not as a scheduled day-before-due-date reminder — no cron/scheduler mechanism exists anywhere in this project, and `BR-010` already leaves the exact reminder timing undefined.

**Notification architecture:** all 20 mandatory Notification Domain Rules from this milestone's kickoff instructions are enforced explicitly: every notification belongs to an existing `user` (Rule 1, FK-enforced); recipients are always resolved from FK-backed queries (`Enrollment`, `ParentStudentLink`, `Student.user_id`, `Teacher.user_id`) before any notification is written, so an invalid/deleted-entity reference is structurally unreachable (Rules 2-3); every dispatch call happens in the originating service **after** that service's own `session.commit()` has already succeeded (Rule 4); duplicate-prevention for attendance warnings is the threshold-crossing semantics above (Rule 5); `GET /notifications`/`PUT .../read` are always scoped to the caller's own `user_id`, with no "view all" mode for any role since none is documented (Rules 6-9, 12-13 not applicable — Admin has no special notification-viewing privilege); `is_read` is a persisted boolean column (Rule 10); mark-as-read is idempotent — a no-op success on an already-read notification (Rule 11); `created_at` is server-generated only (`func.now()`, Rule 12); no schema-hidden information is returned (Rule 13 — the response shape is exactly `{id, type, message, is_read, created_at}`); dispatch failures are caught and logged inside the dispatcher, never propagating to break the caller's already-successful business operation (Rule 14); the batch dispatches (schedule-change to a class roster, fee-due to a student + parents) write one notification per already-resolved recipient with no per-recipient validation step that can fail independently (Rule 15); feed ordering is `created_at DESC` (Rule 16); every message is a fixed server-side template, never a frontend string (Rule 17); FK references (`user_id`) are enforced at the schema level (Rule 18); the four documented triggers are the only automatic ones — no other business event dispatches a notification (Rule 19); all previously-implemented modules were re-verified working via the full regression suite (Rule 20).

**Database:** `notification` table matching `Database_Design.md` §6.26 column-for-column. Alembic revision `0010_notifications` (down_revision `0009`). `Index`/server-default declared on the model itself — `alembic revision --autogenerate` produced an empty diff on the first attempt.

**Backend:** `notification_repository.py` (new) — all SQLAlchemy queries for `notification`. `app/notifications/dispatcher.py` (new, per the roadmap's own file list): the four trigger functions, each independently committing and independently fail-safe. `app/services/notification_service.py` (new — not in the roadmap's literal file list, but required by CLAUDE.md §6's layering rule, same precedent as Milestone 2's un-listed `user_repository.py`): the read-side service backing the `notifications` router. `app/repositories/user_repository.py` extended with `list_parent_user_ids_for_student` (reverse of the existing `parent_has_linked_student`), needed to fan out `fee_due` notifications. Dispatch wired into the four originating services: `result_service.approve_or_reject` (on `decision: approve`), `schedule_service.update_entry`/`delete_entry` (recipients: enrolled students + assigned Teacher), `attendance_service.mark_attendance` (per-student, per-class-session before/after percentage comparison using the existing `_percentage()` helper), `fee_service.create_fee_structure` (per newly-invoiced student). `notifications.py` router: 2 endpoints (`GET /notifications`, `PUT /notifications/{id}/read`).

**Frontend:** `features/notifications/index.ts` (React Query hooks for both endpoints). `pages/Notifications/index.tsx`: chronological feed, unread visual distinction, "Mark all as read," per-item click marks read and deep-links by `notification.type` (Results/Attendance/Timetable/Fee Centre), matching `UI_Wireframes.md` §16. `components/AppLayout.tsx`: a persistent notification bell with an unread-count badge on every authenticated page — a documented simplification links it directly to the full page rather than also building the wireframe's optional desktop dropdown panel (same class of simplification as Milestone 5's Attendance Calendar view).

**Tests:** 25 new tests — 15 unit tests (`dispatcher` functions and `NotificationService`, repositories stubbed) covering every trigger's template/recipient set, the Domain-Rule-14 fail-safe behavior (an exception inside dispatch rolls back its own transaction and never propagates), and the ownership/idempotency rules; 10 integration tests (full request → DB → response cycle against a disposable database) covering all four triggers end-to-end, including the exact threshold-crossing sequence (no warning at exactly 80%, one warning on crossing below, no repeat while still below) and the result-approval-vs-rejection notify/don't-notify distinction. Two pre-existing unit tests (Milestone 7's `test_approve_success`, Milestone 8's `test_rule3_invoices_generated_only_for_eligible_students`) were updated to account for the new dispatch calls those services now make after their own commits — not a behavior regression, just this milestone's additional dispatch. Full suite: **318 passed** (0 failed) — a full regression run across Milestones 1-9.

**Verified (not just claimed):** `pytest` 318/318 against a disposable `ict_education_m9_test` database, created and dropped solely for this run. `pip check` clean. `alembic upgrade head`/`downgrade -1`/`upgrade head` all clean via the bare `alembic` console-script entry point. `alembic revision --autogenerate` produced an empty migration on the first attempt (temporary diff-check file deleted immediately after inspection). `npx tsc --noEmit` and `npm run build` both clean. Backend boots and serves real HTTP via a live `uvicorn` process: `GET /health` → `200 {"status":"ok","database":"ok"}`, wrong-password login → `401` standard envelope, an unauthenticated `GET /notifications` → `401`. `/openapi.json` fetched live and confirmed to list both new endpoints — Swagger verification passed. A dedicated live smoke test (via `TestClient` against a disposable database, before the formal test suite was written) walked the entire cross-milestone integration end-to-end: schedule update/delete notifying both roles, the exact attendance threshold-crossing sequence, fee-structure creation notifying student + linked parent, and the full exam-grade-to-result-to-notification pipeline (M6 exam → M7 result approval → M9 notification) — all via the same disposable database, exercising every prior milestone's real endpoints alongside the new ones with no regression. All disposable databases (`ict_education_m9_dev`, `ict_education_m9_test`) dropped after use — confirmed via a `pg_database` query that none remain; the developer's real `university_management_db` was never touched. `backend/.env`, `frontend/.env`, `backend/.env.local` SHA-256 hashes were recorded before this milestone began and re-checked after every phase — unchanged throughout (`frontend/.env.local` does not exist). No preview tooling was used this milestone.

**Known issues / gaps (tracked, not oversights):**
1. FR-044's scheduled-reminder half (a reminder some days before `due_date`) is not implemented — no cron/scheduler mechanism exists anywhere in this project; only the event-driven `fee_due` notification at invoice-issuance time is built.
2. The notification bell links directly to the full Notifications page rather than also offering the wireframe's optional desktop dropdown panel — a documented simplification, same class as Milestone 5's Attendance Calendar view.
3. `POST /fees/overdue/notify` (FR-056, manual overdue-notify action) is explicitly Milestone 10 scope per `Implementation_Roadmap.md`'s own note — not built here even though the `notification` table it needs now exists.
4. Migration `0010_notifications` is hand-authored, not `alembic revision --autogenerate`'d, though its upgrade/downgrade cycle and an autogenerate diff-check are both confirmed clean.
5. Frontend UI not visually exercised in a browser this milestone (same standing limitation as M3-M8) — verification was `tsc`/`npm run build`/code review only.

**Commits (`8258ea7`..`04eef50`):** `9e5cf7e` (pre-implementation: notification trigger/recipient resolutions), `883007c` (backend: model, migration, repository, dispatcher, service, router, integration into four originating services), `b71b6f5` (frontend: notifications feed, bell icon), `04eef50` (backend test suite).

**Full documentation self-review run** (`CLAUDE.md` §14 item 12, `MILESTONE_VERIFICATION_CHECKLIST.md`): `Database_Design.md` required no changes (schema matched exactly). `API_Contract.md` updated in the same change as each resolution (FR-051 correction, threshold-crossing semantics, invoice-issuance timing, the new §8.3 trigger/template table). `Requirement_Traceability_Matrix.md` FR-031/FR-044/FR-051/FR-052/FR-053 updated, with FR-044 honestly marked partially implemented. `Requirement_Analysis.md` FR-051 corrected. `Implementation_Roadmap.md` gained a scope note recording all three resolutions in the same change. No undocumented additions found beyond what's logged above (the un-listed `notification_service.py` follows the same layering-rule precedent already established in Milestone 2).

**Outstanding before M10 can start:** explicit approval of M9. **Approved by user, git tag `v1.0-milestone9`.**

### M10 — Dashboards & Reporting (Completed — 2026-07-22)

Implemented exactly the scope `Implementation_Roadmap.md` defines for Milestone 10: `GET /results/reports`, `GET /fees/reports` (result/fee reporting, reusing `GET /attendance/reports` unchanged rather than duplicating it), `POST /fees/overdue/notify`, and the four role-specific Dashboards plus the Admin: Reports page. Nothing from Milestone 11 (Hardening, Testing & Deployment) was implemented — confirmed by inspecting `Implementation_Roadmap.md`'s Milestone 11 file list before starting; no such files exist yet in the codebase.

**Pre-implementation review, before any code was written:** read all governing documents and the full codebase, then reported five findings per the user's explicit instruction to stop before coding, all approved as-recommended:
1. **Finding A:** additive `average_gpa` field on `GET /results/reports`, reusing the credit-hour-weighted GPA formula already implemented for `GET /results/me` rather than re-deriving it — `compute_credit_weighted_gpa` was extracted out of `result_service.py`'s private per-semester logic into a shared public function, called by both endpoints. Only `published` results contribute (BR-002).
2. **Finding B:** no backend endpoint added for the Teacher Dashboard's Pending Grading count — computed client-side from the existing per-exam `GET /exams/{id}/results` endpoint (via `useQueries` over the Teacher's own not-yet-published exams), no backend scope expansion.
3. **Finding C:** no endpoint added for a Teacher's own schedule-change-request status — the widget is omitted from the Teacher Dashboard entirely and documented as a known limitation in `UI_Wireframes.md` Section 2, rather than inventing an undocumented endpoint.
4. **Finding D:** additive `created_at` field on `GET /users/students`/`GET /users/teachers` (sourced from `user.created_at`, which already existed) — no business-logic, schema, or new-endpoint change — backing the Admin Dashboard's Recent User Signups widget.
5. **Finding E:** the Parent Dashboard implements only Fee Status and Recent Results (both already Parent-accessible via `GET /fees/me`/`GET /results/me` with a `student_id`, verified against `parent_student_link`); Attendance % and Upcoming Exams render an honest "Not available" state rather than fabricated data, since no endpoint exposes either to the Parent role. Because no endpoint anywhere enumerates a Parent's linked children (the same known gap noted since Milestones 7–8's Results View/Fee Centre), the Parent Dashboard uses a manual Student ID field to select which linked child to view.

**Backend:** `app/schemas/report.py` (new — `ResultsReportResponse`/`FeesReportResponse`/`ReportScope`, not in the roadmap's literal file list but required by `CLAUDE.md` §6, same precedent as Milestone 9's un-listed `notification_service.py`), `app/services/report_service.py` (filled in — `ReportService.get_results_report`/`get_fees_report`, reusing `compute_credit_weighted_gpa` and `fee_service._derived_status` rather than duplicating either), `app/routers/reports.py` (new — hosts both new routes under a router with no own prefix, since `/results` and `/fees` are already owned by `results.router`/`fees.router`), wired into `app/main.py`. Pass/fail determination for `GET /results/reports` (`grade_point > 0` = pass, `== 0` = fail) is a documented engineering interpretation, since `grade_letter` is Teacher-supplied free text with no fixed enum. `total_overdue` is a subset of `total_outstanding`, matching the Admin: Fee Dashboard's existing (Milestone 8) semantics for the same two terms. `ResultRepository.list_published_for_report`/`FeeRepository.list_invoices_for_report` (new, filtered by department/semester/student). `POST /fees/overdue/notify`: `FeeService.notify_overdue_accounts` (new) validates the whole `scope: "selected"` batch against currently-overdue invoices before writing anything (Domain Rule 15 pattern), then calls the existing `dispatcher.notify_fee_due` unchanged for each notified invoice — no second notification system, no bypass of the Notification Dispatcher. `StudentRead`/`TeacherRead` schemas gained `created_at`, populated in `user_service.py`'s `_student_to_read`/`_teacher_to_read` from `user.created_at`.

**Frontend:** `features/attendance/index.ts` gained `useAttendanceReports` (wraps the existing `GET /attendance/reports`, unchanged from Milestone 5), `features/results/index.ts` gained `useResultsReport`, `features/fees/index.ts` gained `useFeesReport`/`useNotifyOverdueAccounts`, `features/users/index.ts`'s `StudentOrTeacher` type gained `created_at`. `pages/Dashboard/`: `StudentDashboard.tsx` (Upcoming Exams, Attendance %, Fee Status, Recent Results — all from existing endpoints already used by ExamList/Attendance/FeeCentre/ResultsView), `TeacherDashboard.tsx` (Classes Today from `GET /schedule/me`; Pending Grading per Finding B), `ParentDashboard.tsx` (per Finding E), `AdminDashboard.tsx` (Pending Result Approvals from `GET /results/pending`, Overdue Fees from `GET /fees/overdue`, Recent User Signups per Finding D), `DashboardCard.tsx` (shared widget-card shell), `index.tsx` rewritten to dispatch by `user.role` — replacing the Milestone 0 placeholder's "Backend connectivity" health-check widget, per the action item carried from the M0 traceability review. `pages/Admin/Reports/index.tsx` (filled in — Attendance/Results/Fees tabs with Department/Semester filters, reusing `GET /attendance/reports` unchanged). `pages/Admin/FeeDashboard/index.tsx` gained the wireframe's per-row "Notify" and "Send Bulk Overdue Notice" actions, deferred since Milestone 8 pending the `notification` table (Milestone 9) and this milestone's own endpoint. `router.tsx`/`AppLayout.tsx` gained the `/admin/reports` route and nav link.

**Tests:** 23 new tests — 15 unit tests (`ReportService` filter-validation/pass-fail-threshold/GPA-reuse/overdue-subset-correctness, 11 tests; `FeeService.notify_overdue_accounts` batch-validation/dispatcher-reuse/scope-selected-vs-all-overdue, 4 tests) with repositories stubbed; 8 integration tests (`test_reports_router.py`, full request → DB → response cycle against a disposable database) covering RBAC on all three new routes, filter validation, an end-to-end published-result → `average_gpa`/`pass_count` verification, fee totals/overdue-subset verification, and `POST /fees/overdue/notify` actually dispatching a real `fee_due` notification via the existing Notification Dispatcher (confirmed via `GET /notifications`, not just the response body). One pre-existing unit-test fixture (`test_user_service.py`'s `make_user`) updated to supply `created_at`, required by the new schema field — not a behavior regression. Full suite: **341 passed** (0 failed) — a full regression run across Milestones 1–10.

**Verified (not just claimed):** `pytest` 341/341, `pip check` clean, `alembic current`/`heads` both at a single head (`0010`, unchanged — Milestone 10 introduces no new tables/migrations), `alembic revision --autogenerate` produces an empty diff (confirmed on a freshly-migrated disposable database, after an initial false-positive caused by the test suite's own `Base.metadata.drop_all` teardown having wiped a database whose `alembic_version` row still said `0010` — re-verified cleanly on a fresh database, not a real drift). `npx tsc --noEmit` and `npm run build` both clean. Backend boots and serves real HTTP via a live `uvicorn` process; `/openapi.json` confirmed to list `GET /results/reports`, `GET /fees/reports`, and `POST /fees/overdue/notify` alongside the existing `GET /attendance/reports`. A full live smoke test (via direct HTTP requests against a disposable database, seeded through the real API — department/course/semester/room/teacher/student setup, class session/enrollment/schedule entry, fee structure with auto-generated invoice, the complete exam → submit → grade → publish → result-submit → approve pipeline) confirmed every Milestone 10 response shape matches its TypeScript hook interface exactly, and that `POST /fees/overdue/notify` correctly produces a `notified_count` and a real dispatched notification. The Vite dev server was also started against this same live backend and confirmed to boot without module/build errors. All disposable databases dropped after use; the developer's real `university_management_db` was never touched. `backend/.env`, `frontend/.env` SHA-256 hashes were recorded before this milestone began and re-checked after every phase — unchanged throughout.

**Known issues / gaps (tracked, not oversights):**
1. **Frontend UI not visually exercised in an interactive browser this milestone.** Per this session's established preview-tooling restriction (traced to a prior `.claude/launch.json` `env`-field incident), `preview_*` MCP tools were not used. Verification relied on `tsc`/`npm run build`/a live API-contract smoke test/code review instead of clicking through the rendered pages — the same class of limitation as Milestones 3–9, but specifically called out here since four new pages (all four Dashboard variants) and one filled-in page (Admin: Reports) were added this milestone.
2. **Teacher Dashboard's Pending Grading count issues one HTTP request per not-yet-published exam** (via `useQueries`), per Finding B's "compute client-side from existing APIs" resolution — acceptable at this system's scale, but not a single aggregate call; a future milestone could add a dedicated endpoint if this becomes a real performance concern.
3. **Parent Dashboard has no linked-children selector**, only a manual Student ID field — the underlying gap (no endpoint anywhere enumerates a Parent's linked children) predates this milestone (Milestones 7–8) and remains open; Finding E's resolution works within that constraint rather than closing it.
4. **Teacher Schedule-change-request status widget is not implemented** (Finding C) — no endpoint exists for a Teacher to query their own pending change-request status; a Teacher's own requests remain visible via the Schedule page.

**Commits:** `66214f1` (backend: report_service.py/routers/reports.py, POST /fees/overdue/notify, created_at additive field, backend test suite), `4092fdc` (frontend: role Dashboards, Admin: Reports page, Fee Dashboard Notify actions), `620c67f` (docs closeout: API_Contract.md, UI_Wireframes.md, Proposal_vs_Engineering_Additions.md, Requirement_Traceability_Matrix.md, PROJECT_PROGRESS.md, CHANGELOG.md).

**Full documentation self-review run** (`CLAUDE.md` §14 item 12, `MILESTONE_VERIFICATION_CHECKLIST.md`): `Database_Design.md` required no changes (no schema changes this milestone). `API_Contract.md` updated for the `average_gpa` addition (§9.1) and the `created_at` addition (§2.3); `POST /fees/overdue/notify` (§6.7) and both report endpoints (§9.1/§9.2) were already fully documented pre-implementation (part of the Project Readiness Audit that added FR-054–FR-056), so needed no further changes beyond the two additive fields. `Proposal_vs_Engineering_Additions.md` §6 gained a note on the `average_gpa`/pass-fail-threshold engineering decisions. `UI_Wireframes.md` Section 2 gained a Known Limitations subsection recording Findings B/C/D/E's resolutions, and Section 18's Results tab description gained the `average_gpa` figure. `Requirement_Traceability_Matrix.md` FR-030/FR-054/FR-055/FR-056 to be updated to Verified in this same documentation pass (see that document directly). No undocumented additions found beyond what's logged above.

**Outstanding before M11 can start:** explicit approval of M10. **Approved by user, git tag `v1.1-milestone10`.**

### M11 — Hardening, Testing & Deployment (Completed — 2026-07-23)

Implemented exactly the scope `Implementation_Roadmap.md` defines for Milestone 11: security/config hardening, test coverage completion, seed data, and CI wiring. Milestones 0–10 were left untouched — confirmed by a fresh regression run (349/349 backend tests) at both the start and end of this milestone, and by never modifying any pre-existing business logic, schema, or endpoint contract.

**Pre-implementation review, before any code was written:** read all governing documents and the full codebase (including a fresh regression baseline: 341/341 backend tests, empty `alembic revision --autogenerate` diff, clean `tsc`/`npm run build`), then reported findings per the user's explicit instruction to stop before coding. Three required a decision, all approved as-recommended:
1. **Finding 1:** the orphaned `pages/Parent/ChildView/index.tsx` placeholder (never built across Milestones 7–10, each time deferred to "a future milestone" that never arrived) — approved to delete as dead-code cleanup, with the underlying FR-032/FR-037 gap recorded as a final, permanent, tracked limitation rather than silently dropped.
2. **Finding 2:** `/docs`/`/redoc`/`/openapi.json` exposure — approved to disable when `ENVIRONMENT=production` via the already-defined (previously unused) `Settings.is_production` property, with every other environment unaffected.
3. **Finding 3:** the pre-existing, never-configured `eslint` devDependency/`lint` script (present since Milestone 0) — approved to complete with a minimal flat config, not remove.

Additional obvious documentation corrections applied directly (no approval needed): `Implementation_Roadmap.md`'s stale M11 file paths (`infra/docker/` → `docker/`, `infra/ci/pipeline.yml` → `.github/workflows/*.yml`) and stale "already exists" notes; `backend/.env.example`'s duplicate `ENVIRONMENT` line; `Database_Design.md` §11's stale "threshold needs confirmation" note (resolved in Milestone 5); `README.md`'s Milestone-0-era status line and Dashboard description.

**Security hardening:** `POST /auth/login` rate limiting (`app/middleware/rate_limit.py`) — 5 attempts per 60-second window, per client IP, in-memory fixed window, resolving `Requirement_Analysis.md` §14 item 13 (a gap the proposal never specifies a policy for). `/docs`/`/redoc`/`/openapi.json` disabled in production (`app/main.py`'s `create_app()`), verified live: `200` in development, `404` in production, `GET /health` unaffected in both. Neither change touches any existing business endpoint, schema, or migration.

**Frontend hardening:** root `ErrorBoundary` (`components/ErrorBoundary.tsx`) + `reportClientError` (`lib/reportClientError.ts`), implementing `System_Architecture.md` §10's "client-side errors are captured via an error boundary" requirement — never implemented in any prior milestone. Minimal ESLint flat config (`eslint.config.js`) — ESLint's own recommended rules, `typescript-eslint`'s recommended rules, and only the two long-standing `eslint-plugin-react-hooks` rules (`rules-of-hooks`, `exhaustive-deps`), deliberately excluding that plugin's newer `set-state-in-effect` rule since enabling it would require refactoring frozen, working Milestone 0–10 code purely to satisfy a new rule — out of scope for a hardening-only milestone. `npm run lint` passes clean against the entire existing `src/` tree.

**Frontend test coverage:** `frontend/tests/` was scaffolded empty since Milestone 0 despite `vitest` being installed — filled in with `@testing-library/react`/`@testing-library/jest-dom`/`@testing-library/user-event`/`jsdom` and 7 component tests covering exactly the three flows `CLAUDE.md` §10 names as critical interaction logic: `ExamRoom` (countdown computed from the server-recorded start time, auto-submit exactly once at zero — 2 tests), `GradingInterface` (mark entry submits the correct payload, VR-006's max-marks 422 shows the specific inline message — 2 tests), `ResultApproval` (approve/reject-requires-comment/reject-with-comment — 3 tests). One real bug found and fixed during this work: the first `GradingInterface` test draft returned a fresh object literal per mock-hook call, which — combined with the page's own `useEffect(..., [detail])` — caused an infinite render loop that exhausted the test worker's memory; fixed by hoisting stable, shared object references for the mocked hook data.

**Seed data:** `backend/scripts/seed_demo_data.py` — was an empty placeholder since Milestone 0 despite `Database_Design.md` §11 specifying 19 required seed items. Implemented all of them except item 11 (the intentional schedule conflict, deliberately excluded from the demo dataset per that section's own note — already covered by the existing schedule-conflict test suite): 2 departments, 2 semesters, 4 rooms, 1 admin, 3 teachers, 8 students (including one crossing the low-attendance threshold and one with an overdue invoice), 2 parents (one linked to two children, exercising the M:N relationship), 5 courses, 6 class sessions, enrollments, schedule entries, 4 exams in each lifecycle state (draft/scheduled/open/published) with a mix of MCQ/short-answer/descriptive/coding questions, one fully-graded and one pending submission, an attendance history, 3 results (submitted/published/rejected), 3 fee structures with paid/partially-paid/overdue invoices, and 4 notifications (one per type, mixed read/unread). Idempotent (checks for the "Computer Science" department first, matching `seed_admin.py`'s own precedent) and writes directly via the ORM session, not the service layer — same precedent as `seed_admin.py`. Verified via a live end-to-end smoke test against a disposable database: all four seeded role logins work, the low-attendance student's `GET /attendance/me` correctly shows `50.0%`/`low_attendance_warning: true`, the published result's GPA/reports match, and re-running the script a second time is a clean no-op.

**CI wiring:** `.github/workflows/backend-ci.yml` and `frontend-ci.yml` were genuine placeholders (`echo "not yet implemented"`) since Milestone 0 — replaced with real pipelines: backend runs against a disposable Postgres service container (`pip check`, `alembic upgrade head`, an autogenerate empty-diff check, `pytest`); frontend runs `tsc --noEmit`, `npm run lint`, `npx vitest run`, `npm run build`. Both workflows validated as syntactically correct YAML; their individual steps were exercised locally throughout this milestone (the CI runner itself was not invoked, since that requires pushing to GitHub Actions — see Known Issues).

**Documentation:** `docs/Proposal_vs_Engineering_Additions.md` gained six new entries in the same commits that introduced each item (rate limiting, docs-gating, ESLint config, error boundary, component tests) — the same-commit policy this document's own header describes. `docs/UI_Wireframes.md` §17 gained a final Known Limitation note for the Parent: Child View screen. `docs/Requirement_Traceability_Matrix.md` FR-032/FR-037 updated to reflect the final (not "deferred") status of that screen. `docs/API_Contract.md` §1.1 documents the new 429 status and rate-limit policy.

**Verified (not just claimed):** `pytest` 349/349 (341 pre-existing + 4 rate-limit unit + 3 app-factory unit + 1 rate-limit integration) against a disposable `ict_education_m11_final` database, created and dropped solely for this run — re-run twice more during the milestone (once after the rate-limiter fix confirming the shared in-memory bucket didn't break existing tests, once as the final pass). `pip check` clean. `alembic current`/`heads` both at the single existing head `0010` (unchanged — no new migrations). `alembic revision --autogenerate` produces an empty diff. `npx tsc --noEmit`, `npm run lint`, `npx vitest run` (7/7), and `npm run build` all clean. Backend boots and serves real HTTP via a live `uvicorn` process in both `development` (docs/redoc/openapi all `200`) and `production` (`ENVIRONMENT=production`; all three `404`, `/health` unaffected) — and the rate limiter was exercised live against a running server: 5 failed login attempts returned `401`, the 6th returned `429`. All disposable databases (`ict_education_m11_baseline`, `ict_education_m11_ratelimit`, `ict_education_m11_seed`, `ict_education_m11_final`) dropped after use; the developer's real `university_management_db` was never touched. `backend/.env`, `backend/.env.local`, `frontend/.env` SHA-256 hashes were recorded before this milestone began and re-checked after every phase — unchanged throughout (`frontend/.env.local` does not exist).

**Known issues / gaps (tracked, not oversights):**
1. **The GitHub Actions CI runner itself was not exercised** — both workflow files are syntactically valid and their individual steps were run locally (matching what CI would do), but actually pushing a commit to trigger a real Actions run is outside what this environment can verify. Recommended before treating CI as fully proven: push a commit and confirm both workflows go green.
2. **The login rate limiter is in-memory and single-process** — it does not coordinate across multiple worker processes or horizontally-scaled instances. Acceptable for this project's documented single-instance deployment target; a production deployment that scales the API tier would need a shared store (e.g. Redis) instead.
3. **The Parent: Child View dedicated screen (`UI_Wireframes.md` §17) is now a permanent, accepted limitation**, not a deferred one — no endpoint anywhere enumerates a Parent's linked children, and building one is new business functionality outside this hardening-only milestone's scope. The underlying data access (FR-032/FR-037/FR-038/FR-041) remains fully implemented and tested.
4. Frontend component test coverage is scoped to the three flows `CLAUDE.md` §10 names explicitly (exam timer, grading form, approval workflow) — not exhaustive coverage of every page.

**Commits:** *(pending — this milestone's implementation, tests, and documentation changes have not yet been committed; will be recorded here once committed, per this project's established per-milestone commit pattern)*

**Full documentation self-review run** (`CLAUDE.md` §14 item 12, `MILESTONE_VERIFICATION_CHECKLIST.md`): `Database_Design.md` required only the one stale-note correction (§11). `API_Contract.md` updated for the rate-limiting addition. `Proposal_vs_Engineering_Additions.md` updated in the same commit as each of the six new items. `Requirement_Traceability_Matrix.md` updated for FR-032/FR-037's final status. `Implementation_Roadmap.md` corrected for the stale M11 file paths, in the same change that revealed them. No undocumented additions found beyond what's logged above.

**Outstanding:** explicit approval of M11 — the final milestone in `Implementation_Roadmap.md`. No further milestones follow.
